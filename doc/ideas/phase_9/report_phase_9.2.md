# 📋 Phase 9.2: Context Compression — Детальный отчёт

**Дата завершения:** 4 декабря 2025  
**Статус:** ✅ Завершено  
**Коммитов:** 3  
**Новых тестов:** 32  
**Общее количество unit-тестов:** 570

---

## 📖 Содержание

1. [Мотивация и проблема](#1-мотивация-и-проблема)
2. [Анализ существующих решений](#2-анализ-существующих-решений)
3. [Архитектурные решения](#3-архитектурные-решения)
4. [Компоненты Phase 9.2](#4-компоненты-phase-92)
5. [Алгоритм сжатия](#5-алгоритм-сжатия)
6. [Интеграция с ChatHistoryManager](#6-интеграция-с-chathistorymanager)
7. [CLI интеграция](#7-cli-интеграция)
8. [Тестирование](#8-тестирование)
9. [Сравнение стратегий](#9-сравнение-стратегий)
10. [Производительность и экономика](#10-производительность-и-экономика)
11. [Ограничения и компромиссы](#11-ограничения-и-компромиссы)
12. [История коммитов](#12-история-коммитов)
13. [Связь с другими фазами](#13-связь-с-другими-фазами)
14. [Дальнейшее развитие](#14-дальнейшее-развитие)

---

## 1. Мотивация и проблема

### 1.1 Проблема контекстного окна

Каждая современная LLM имеет ограничение на размер контекстного окна — максимальное 
количество токенов, которое модель может обработать за один запрос. Для Gemini 2.0 Flash 
это около 1 миллиона токенов, но практические ограничения гораздо жёстче:

**Экономические ограничения:**
- Стоимость запроса пропорциональна количеству входных токенов
- Длинная история = дорогой каждый запрос
- При активном чате расходы растут экспоненциально

**Технические ограничения:**
- Latency увеличивается с размером контекста
- "Потеря иголки в стоге сена" — модели хуже работают с очень длинным контекстом
- Качество ответов может падать при переполненном контексте

### 1.2 Сценарий проблемы

Представим типичную сессию RAG-чата:

```
Сообщение 1: "Расскажи об архитектуре проекта" (50 токенов)
Ответ 1: [Детальное описание] (500 токенов)

Сообщение 2: "Как работает поиск?" (20 токенов)
Ответ 2: [Объяснение гибридного поиска] (600 токенов)

... 20 сообщений спустя ...

Сообщение 21: "А что такое RRF?"
```

К 21-му сообщению история может содержать 15,000+ токенов. При этом:
- 80% истории — устаревший контекст
- Пользователь уже забыл о чём говорили в начале
- Модель тратит ресурсы на обработку нерелевантной информации

### 1.3 Решение Phase 9.1 vs Phase 9.2

**Phase 9.1 (простые стратегии):**
- `LastNMessages` — хранит N последних сообщений
- `TokenBudget` — ограничивает по токенам
- Проблема: теряем контекст полностью

**Phase 9.2 (умное сжатие):**
- `AdaptiveWithCompression` — сжимает старые сообщения через LLM
- Сохраняем ключевые факты в компактном виде
- Новые сообщения остаются полными

---

## 2. Анализ существующих решений

### 2.1 Подходы в индустрии

**Truncation (отсечение):**
- Самый простой подход
- Полная потеря контекста
- Используется: ChatGPT (по умолчанию), Claude

**Sliding Window:**
- Храним последние N токенов
- Плавная потеря старого контекста
- Используется: многие чат-боты

**Summarization:**
- LLM сжимает старую историю
- Сохраняем ключевые факты
- Используется: MemGPT, некоторые enterprise решения

**Hierarchical Memory:**
- Несколько уровней памяти (short-term, long-term)
- Сложная реализация
- Используется: исследовательские проекты

### 2.2 Почему мы выбрали Summarization

**Преимущества:**
1. Сохраняет семантику, а не просто символы
2. Естественная интеграция с LLM-based системой
3. Пользователь не теряет контекст предыдущих обсуждений
4. Можно настраивать степень сжатия

**Компромиссы:**
1. Дополнительный вызов LLM (стоимость)
2. Потенциальная потеря деталей при агрессивном сжатии
3. Latency на момент сжатия

### 2.3 Наш подход: Adaptive Compression

Мы реализовали адаптивное сжатие с двумя порогами:

- `threshold_tokens` — порог срабатывания (когда начинаем сжимать)
- `target_tokens` — целевой размер после сжатия

Это позволяет:
- Не сжимать при каждом сообщении (экономия)
- Контролировать степень сжатия
- Накапливать summary из нескольких сжатий

---

## 3. Архитектурные решения

### 3.1 Разделение ответственности

Мы разделили функциональность на два компонента:

**ContextCompressor:**
- Отвечает только за сжатие списка сообщений
- Не знает о стратегиях и порогах
- Использует LLM для генерации summary
- Чистая функция: messages → summary_message

**AdaptiveWithCompression:**
- Реализует `BaseChatHistoryStrategy`
- Знает о порогах и логике срабатывания
- Использует `ContextCompressor` для сжатия
- Хранит состояние (текущий summary)

### 3.2 Почему не один класс?

Разделение даёт нам:

1. **Тестируемость** — можем тестировать сжатие отдельно от логики порогов
2. **Переиспользование** — ContextCompressor можно использовать для других целей
3. **Single Responsibility** — каждый класс делает одну вещь
4. **Mockability** — легко подменить компрессор в тестах

### 3.3 Расширение интерфейса стратегии

Мы добавили новый метод в `AdaptiveWithCompression`:

```
get_full_context(messages) → [summary] + messages
```

Этот метод возвращает полный контекст для LLM, включая сжатое summary.
Другие стратегии (LastNMessages, TokenBudget) не имеют этого метода.

### 3.4 Duck Typing vs ABC

Мы использовали duck typing для `get_full_context`:

```
if hasattr(strategy, "get_full_context"):
    full_messages = strategy.get_full_context(messages)
```

**Почему не добавили в ABC?**
- Не все стратегии имеют summary
- Обратная совместимость с существующими стратегиями
- YAGNI — не усложняем интерфейс до необходимости

---

## 4. Компоненты Phase 9.2

### 4.1 ContextCompressor

**Расположение:** `semantic_core/core/context/compressor.py`

**Назначение:** Сжатие списка сообщений чата в одно summary-сообщение через LLM.

**Ключевые характеристики:**

1. **Промпт для сжатия:**
   - Инструкции по сохранению ключевых фактов
   - Сохранение технических деталей и значений
   - Краткость (2-3 параграфа максимум)
   - Сохранение языка оригинальной беседы

2. **Формат вывода:**
   - Роль: `system`
   - Префикс: `[Previous conversation summary]`
   - Токены: из ответа LLM или оценка (len/4)

3. **Температура:**
   - По умолчанию 0.3 (низкая)
   - Для детерминированности результата
   - Меньше "креатива", больше точности

### 4.2 AdaptiveWithCompression

**Расположение:** `semantic_core/core/context/strategies.py`

**Назначение:** Стратегия автоматического сжатия при достижении порога токенов.

**Параметры:**
- `compressor` — экземпляр ContextCompressor
- `threshold_tokens` — порог срабатывания (по умолчанию 30,000)
- `target_tokens` — целевой размер (по умолчанию 10,000)

**Инварианты:**
- `threshold_tokens > target_tokens` (иначе бесконечный цикл)
- `target_tokens >= 1`

**Состояние:**
- `_summary` — текущее сжатое summary (или None)

### 4.3 Обновления ChatHistoryManager

**Новые методы/свойства:**

1. **`has_summary`** — проверяет, есть ли сжатый summary в стратегии
2. **`total_tokens()`** — теперь включает токены summary
3. **`get_messages_for_llm()`** — автоматически добавляет summary если есть

---

## 5. Алгоритм сжатия

### 5.1 Когда срабатывает сжатие

Сжатие запускается при выполнении условия:

```
total_tokens(messages) + summary_tokens > threshold_tokens
```

Где:
- `total_tokens(messages)` — сумма токенов всех текущих сообщений
- `summary_tokens` — токены текущего summary (0 если нет)
- `threshold_tokens` — настроенный порог

### 5.2 Алгоритм разделения сообщений

При срабатывании сжатия история делится на две части:

**to_keep (сохранить):**
- Последние сообщения в пределах `target_tokens`
- Идём с конца, добавляем пока не превысим бюджет
- Эти сообщения остаются без изменений

**to_compress (сжать):**
- Все остальные сообщения
- Плюс текущий summary (если есть)
- Отправляются в LLM для сжатия

### 5.3 Пример работы алгоритма

**Начальное состояние:**
```
Порог: 1000 токенов
Цель: 400 токенов
Summary: None

История:
- msg1 (user): 200 токенов
- msg2 (assistant): 300 токенов  
- msg3 (user): 250 токенов
- msg4 (assistant): 350 токенов

Всего: 1100 токенов > 1000 → СЖАТИЕ
```

**Разделение:**
```
to_keep (с конца, пока ≤ 400):
- msg4: 350 ✓ (350 < 400)
- msg3: 250 ✗ (350 + 250 = 600 > 400)

to_compress:
- msg1: 200
- msg2: 300
- msg3: 250

→ LLM сжимает to_compress → summary (100 токенов)
```

**Результат:**
```
Summary: 100 токенов (содержит суть msg1-msg3)
История: [msg4] = 350 токенов
Всего: 450 токенов
```

### 5.4 Накопительное сжатие

При следующем сжатии старый summary включается в to_compress:

```
to_compress = [old_summary] + [new_old_messages]
new_summary = LLM.compress(to_compress)
```

Это означает:
- Контекст никогда не теряется полностью
- Summary накапливает информацию
- Модель видит "сжатую историю" всей беседы

---

## 6. Интеграция с ChatHistoryManager

### 6.1 Прозрачная интеграция

`ChatHistoryManager` работает одинаково со всеми стратегиями:

```
manager.add_user("Привет!")
manager.add_assistant("Здравствуйте!")
```

Логика сжатия скрыта внутри стратегии и срабатывает автоматически.

### 6.2 Получение контекста для LLM

Метод `get_messages_for_llm()` теперь умный:

1. Проверяет, есть ли у стратегии `get_full_context`
2. Если да — вызывает его (добавляет summary)
3. Если нет — возвращает только messages

Это позволяет CLI и RAGEngine не знать о деталях стратегии.

### 6.3 Подсчёт токенов

Метод `total_tokens()` теперь учитывает summary:

```
total = sum(m.tokens for m in messages)
if strategy has summary:
    total += strategy.summary.tokens
```

Это важно для корректного отображения статистики пользователю.

---

## 7. CLI интеграция

### 7.1 Новые флаги команды chat

**`--compress-at N`:**
- Порог токенов для автоматического сжатия
- Переключает стратегию на `AdaptiveWithCompression`
- Пример: `--compress-at 30000`

**`--compress-target N`:**
- Целевое количество токенов после сжатия
- Используется только с `--compress-at`
- По умолчанию: 10000
- Пример: `--compress-at 30000 --compress-target 5000`

### 7.2 Приоритет стратегий

CLI выбирает стратегию в следующем порядке:

1. `--no-history` → отключает историю
2. `--compress-at` → AdaptiveWithCompression
3. `--token-budget` → TokenBudget
4. По умолчанию → LastNMessages(n=history_limit)

### 7.3 Отображение статуса сжатия

После каждого ответа CLI показывает:

```
Токены: 1250 (input: 800, output: 450) | история: 5 сообщ., 2100 токенов (сжато)
```

Флаг `(сжато)` появляется когда `has_summary == True`.

### 7.4 Приветственное сообщение

При `--compress-at`:
```
История: [cyan]сжатие при 30000 токенов[/cyan]
```

---

## 8. Тестирование

### 8.1 Структура тестов

**test_compressor.py (13 тестов):**
- TestContextCompressor — основная функциональность
- TestContextCompressorIntegration — интеграционные сценарии

**test_strategies.py (16 новых тестов):**
- TestAdaptiveWithCompression — все аспекты стратегии

**test_manager.py (6 новых тестов):**
- TestChatHistoryManagerWithCompression — интеграция

### 8.2 Тестовые сценарии ContextCompressor

| Тест | Что проверяет |
|------|---------------|
| `test_init` | Инициализация с LLM |
| `test_init_custom_temperature` | Кастомная температура |
| `test_compress_empty_messages` | Пустой список → пустой summary |
| `test_compress_single_message` | Одно сообщение |
| `test_compress_multiple_messages` | Несколько сообщений |
| `test_compress_formats_history_correctly` | Формат USER/ASSISTANT |
| `test_compress_uses_output_tokens` | Использование токенов от LLM |
| `test_compress_estimates_tokens_if_none` | Оценка если нет токенов |
| `test_compress_prompt_contains_rules` | Правила в промпте |
| `test_format_history_separates_messages` | Разделение сообщений |
| `test_estimate_tokens` | Оценка токенов (len/4) |

### 8.3 Тестовые сценарии AdaptiveWithCompression

| Тест | Что проверяет |
|------|---------------|
| `test_init_valid` | Валидные параметры |
| `test_init_invalid_threshold_lte_target` | threshold ≤ target → ошибка |
| `test_init_invalid_target_zero` | target = 0 → ошибка |
| `test_should_trim_under_threshold` | Под порогом → False |
| `test_should_trim_over_threshold` | Над порогом → True |
| `test_should_trim_includes_summary_tokens` | Учитывает токены summary |
| `test_trim_separates_to_compress_and_keep` | Разделение сообщений |
| `test_trim_creates_summary` | Создание summary |
| `test_trim_includes_old_summary` | Включение старого summary |
| `test_trim_no_compression_if_all_fits` | Всё умещается → без сжатия |
| `test_get_full_context_without_summary` | Без summary → только messages |
| `test_get_full_context_with_summary` | С summary → [summary] + messages |
| `test_summary_property` | Property summary работает |

### 8.4 Тестовые сценарии ChatHistoryManager + Compression

| Тест | Что проверяет |
|------|---------------|
| `test_has_summary_false_for_simple_strategies` | False для LastN/TokenBudget |
| `test_has_summary_false_before_compression` | False до сжатия |
| `test_has_summary_true_after_compression` | True после сжатия |
| `test_total_tokens_includes_summary` | Токены summary включены |
| `test_get_messages_for_llm_with_summary` | Summary в начале |
| `test_get_messages_for_llm_without_summary` | Работает без summary |

### 8.5 Mock LLM Provider

Для тестов используется MockLLMProvider:

- Настраиваемый response_text
- Настраиваемые output_tokens
- Счётчик вызовов (call_count)
- Сохранение последнего промпта (last_prompt)

Это позволяет:
- Тестировать без реального API
- Проверять параметры вызова
- Контролировать ответы

---

## 9. Сравнение стратегий

### 9.1 Таблица сравнения

| Критерий | LastNMessages | TokenBudget | AdaptiveWithCompression |
|----------|---------------|-------------|-------------------------|
| Сложность | Простая | Средняя | Высокая |
| Потеря контекста | Полная | Полная | Частичная |
| Доп. вызовы LLM | 0 | 0 | При сжатии |
| Настраиваемость | 1 параметр | 1 параметр | 3 параметра |
| Память | O(N) | O(N) | O(N) + summary |
| Предсказуемость | Высокая | Высокая | Средняя |

### 9.2 Когда какую использовать

**LastNMessages:**
- Простые чаты без долгого контекста
- Когда важна предсказуемость
- Отладка и тестирование

**TokenBudget:**
- Контроль расходов
- Известный бюджет на запрос
- Фиксированные лимиты

**AdaptiveWithCompression:**
- Долгие сессии с важным контекстом
- RAG с накоплением знаний
- Когда качество важнее экономии

### 9.3 Комбинирование стратегий

В будущем можно добавить композитные стратегии:

- `CompositeStrategy([TokenBudget, AdaptiveWithCompression])`
- Сначала проверяем бюджет, потом сжимаем если нужно

---

## 10. Производительность и экономика

### 10.1 Стоимость сжатия

**Входные токены (prompt):**
- Инструкция сжатия: ~100 токенов
- История для сжатия: threshold - target токенов
- Например: 30,000 - 10,000 = 20,000 токенов

**Выходные токены (summary):**
- Обычно 200-500 токенов
- Зависит от плотности информации

**Стоимость (Gemini 2.0 Flash):**
- Input: $0.075 / 1M токенов
- Output: $0.30 / 1M токенов
- Одно сжатие 20k токенов: ~$0.0015 + summary

### 10.2 ROI сжатия

**Без сжатия (после 50 сообщений):**
- Каждый запрос: ~50,000 входных токенов
- Стоимость запроса: ~$0.00375

**С сжатием:**
- Каждый запрос: ~15,000 входных токенов (10k сообщений + 5k summary)
- Стоимость запроса: ~$0.001125
- Экономия: 70% на запрос

**Точка безубыточности:**
- Сжатие окупается после 2-3 запросов

### 10.3 Latency

**Момент сжатия:**
- Дополнительный вызов LLM: 1-3 секунды
- Происходит редко (при достижении порога)

**Обычные запросы:**
- Меньше входных токенов → быстрее ответ
- Gemini обрабатывает 10k токенов быстрее чем 50k

---

## 11. Ограничения и компромиссы

### 11.1 Потеря деталей

**Проблема:**
Сжатие неизбежно теряет часть информации. LLM решает, что важно сохранить.

**Митигация:**
- Чёткие инструкции в промпте (сохранять технические детали)
- Низкая температура (0.3) для детерминированности
- Разумные пороги (не сжимать слишком агрессивно)

### 11.2 Накопление ошибок

**Проблема:**
При многократном сжатии ошибки могут накапливаться:
- summary_1 → summary_2 → summary_3
- Каждое сжатие потенциально теряет/искажает информацию

**Митигация:**
- Разумные пороги (сжатие происходит редко)
- Качественный промпт для сжатия
- Возможность начать с чистой истории (/clear)

### 11.3 Непредсказуемость

**Проблема:**
LLM может генерировать разные summary для одной истории.

**Митигация:**
- Низкая температура (0.3)
- Чёткие инструкции
- Тестирование на типичных сценариях

### 11.4 Зависимость от LLM

**Проблема:**
Сжатие требует рабочего LLM. Если API недоступен — стратегия не работает.

**Митигация:**
- Graceful degradation (можно fallback на TokenBudget)
- Retry логика в GeminiLLMProvider
- Сжатие не критично — можно пропустить

---

## 12. История коммитов

### Коммит 1: feat: Добавлен ContextCompressor и AdaptiveWithCompression

**Файлы:**
- `semantic_core/core/context/compressor.py` (новый)
- `semantic_core/core/context/strategies.py` (обновлён)
- `semantic_core/core/context/__init__.py` (обновлён)

**Изменения:**
- Создан ContextCompressor с промптом для сжатия
- Добавлена стратегия AdaptiveWithCompression
- Обновлены экспорты модуля

### Коммит 2: feat: CLI chat поддерживает адаптивное сжатие истории

**Файлы:**
- `semantic_core/core/context/manager.py` (обновлён)
- `semantic_core/cli/commands/chat.py` (обновлён)

**Изменения:**
- ChatHistoryManager: has_summary, улучшенный total_tokens
- CLI: флаги --compress-at, --compress-target
- Отображение статуса сжатия в REPL

### Коммит 3: test: Добавлены тесты для Phase 9.2 Context Compression

**Файлы:**
- `tests/unit/core/context/test_compressor.py` (новый)
- `tests/unit/core/context/test_strategies.py` (обновлён)
- `tests/unit/core/context/test_manager.py` (обновлён)

**Изменения:**
- 13 тестов для ContextCompressor
- 16 тестов для AdaptiveWithCompression
- 6 тестов интеграции с ChatHistoryManager
- Всего: 570 unit-тестов (было 538)

---

## 13. Связь с другими фазами

### 13.1 Зависимости

**Phase 9.0 (Core RAG):**
- BaseLLMProvider — используется ContextCompressor
- GeminiLLMProvider — реальный провайдер для сжатия
- RAGEngine — передаёт историю

**Phase 9.1 (Context Management):**
- ChatMessage — DTO для сообщений
- BaseChatHistoryStrategy — базовый интерфейс
- ChatHistoryManager — управление историей

### 13.2 Что использует Phase 9.2

**Phase 9.3 (Slash Commands):**
- `/compress` — принудительное сжатие
- `/tokens` — отображение статистики с учётом summary

### 13.3 Архитектурная схема

```
┌─────────────────────────────────────────────────────────────┐
│                       CLI chat                              │
│  --compress-at 30000 --compress-target 10000                │
└──────────────────────────┬──────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────┐
│                  ChatHistoryManager                         │
│  ┌─────────────────────────────────────────────────────┐    │
│  │           AdaptiveWithCompression                   │    │
│  │  threshold: 30000  target: 10000  summary: [...]    │    │
│  │                        │                            │    │
│  │                        ▼                            │    │
│  │              ContextCompressor                      │    │
│  │                        │                            │    │
│  │                        ▼                            │    │
│  │              GeminiLLMProvider                      │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

---

## 14. Дальнейшее развитие

### 14.1 Возможные улучшения

**Умное сжатие по темам:**
- Определять темы в истории
- Сжимать только закрытые темы
- Сохранять полный контекст текущей темы

**Кэширование summary:**
- Сохранять summary между сессиями
- Продолжать разговор с прошлой точки

**Настраиваемый промпт:**
- Позволить пользователю задать свой промпт для сжатия
- Адаптация под специфику домена

### 14.2 Интеграция с Phase 9.3

Phase 9.3 добавит:
- `/compress` — принудительное сжатие в любой момент
- `/tokens` — детальная статистика
- `/history` — просмотр истории (включая summary)

### 14.3 Метрики для мониторинга

- Частота сжатий за сессию
- Средний размер summary
- Compression ratio (input/output)
- Время на сжатие
- Качество ответов после сжатия

---

## Заключение

Phase 9.2 добавила умное сжатие контекста чата через LLM summarization. 
Это позволяет вести долгие сессии RAG-чата без потери важного контекста 
и без экспоненциального роста расходов на токены.

Ключевые достижения:
- **ContextCompressor** — универсальный компонент для сжатия истории
- **AdaptiveWithCompression** — стратегия с автоматическим срабатыванием
- **Прозрачная интеграция** — ChatHistoryManager работает одинаково со всеми стратегиями
- **CLI флаги** — простое включение сжатия через --compress-at
- **32 новых теста** — полное покрытие функциональности

Фаза завершена с 3 атомарными коммитами и полной документацией.
