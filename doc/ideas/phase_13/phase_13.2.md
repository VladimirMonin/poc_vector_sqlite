[Image of reusable software component diagram]

Это отличный подход. **Ручное тестирование (User Acceptance Testing)** — это единственный способ убедиться, что система ощущается "живой" и "умной", а не просто "проходит тесты".

Я составил для тебя **Протокол Тотального Тестирования (Phase 13.2)**. Это пошаговый сценарий, который ты проходишь руками в терминале.

-----

# 🕵️‍♂️ Phase 13.2: Human-First Testing Protocol

**Цель:** Визуально подтвердить работу всех подсистем (Ingest, Search, RAG, Media) через CLI.
**Инструмент:** Терминал (PowerShell/Bash) + Твои глаза.

-----

## 📂 0. Подготовка Полигона

Создай папку `test_playground/` и положи туда файлы для теста.

**Необходимые ассеты (создай или найди):**

1. **`01_simple.md`**: Простой текст. (Например, пару абзацев про Python).
2. **`02_structure.md`**: Текст с заголовками H1/H2/H3 и блоками кода (` python ... `).
3. **`03_media.md`**: Текст, содержащий ссылки:
      * `![Диаграмма БД](assets/db_schema.png)`
      * `[Запись встречи](assets/meeting.mp3)`
4. **Папка `assets/`**:
      * `db_schema.png` (Любая картинка со схемой или текстом).
      * `meeting.mp3` (Короткая запись голоса, 10-15 сек).
      * `demo.mp4` (Короткое видео).

**Инициализация (Сброс базы):**

```powershell
# Удаляем старую базу, чтобы тест был чистым
Remove-Item semantic.db -ErrorAction SilentlyContinue
# Инициализируем конфиг
semantic init --non-interactive
```

-----

## 🧪 Сценарий 1: Ingestion (Загрузка и Нарезка)

Проверяем, как система "ест" контент.

**Действие:**

```powershell
# Загружаем всё рекурсивно в синхронном режиме (чтобы видеть логи)
semantic ingest ./test_playground --recursive --mode sync
```

**👀 Что проверять глазами:**

1. **Log Output:** Видишь ли ты эмодзи `📥`, `🧶`, `✂️`?
2. **Media Processing:**
      * Для `db_schema.png`: Должно быть написано `👁️ Analyzed Image`.
      * Для `meeting.mp3`: Должно быть `🎙️ Transcribed Audio`.
3. **Ошибки:** Не должно быть красных Traceback'ов.

-----

## 🧪 Сценарий 2: Search (Проверка "Мозгов" и FTS)

Проверяем, что поиск находит то, что нужно, и ранжирует правильно.

**Тест 2.1: Поиск по смыслу (Vector)**

* *Запрос:* (Спроси синонимами, которых нет в тексте). *Например, если в тексте "Python function", ищи "creating methods".*

    ```powershell
    semantic search "creating methods" --type vector --limit 3
    ```

* **👀 Проверка:** Должен найтись чанк с кодом или описанием функции. Score должен быть \> 0.6.

**Тест 2.2: Поиск по точному вхождению (FTS)**

* *Запрос:* (Уникальное слово из твоего текста, например `db_schema` или имя переменной в коде).

    ```powershell
    semantic search "db_schema" --type fts
    ```

* **👀 Проверка:** Должен найтись именно этот чанк. Если список пуст — FTS сломан.

**Тест 2.3: Гибридный поиск (RRF)**

* *Запрос:* (Комбинация смысла и точного слова). *Например: "схема базы данных db\_schema"*

    ```powershell
    semantic search "схема базы данных db_schema" --type hybrid
    ```

* **👀 Проверка:** Чанк с картинкой (или ссылкой на неё) должен быть **Топ-1**. Это подтверждает, что работает бустинг RRF (13.1).

**Тест 2.4: Поиск Медиа**

* *Запрос:* (То, что нарисовано на картинке или сказано в аудио).

    ```powershell
    semantic search "таблица пользователей" --type image
    semantic search "привет всем" --type audio
    ```

* **👀 Проверка:**
  * Для image: Должен найтись `IMAGE_REF` чанк с путем к картинке.
  * Для audio: Должен найтись `AUDIO_REF` чанк с транскрипцией.

-----

## 🧪 Сценарий 3: Chat & RAG (Интерактив)

Запускаем: `semantic chat`

**Тест 3.1: Контекст Чанков**

* *Ввод:* "Что изображено на схеме базы данных?"
* *Ожидание:* Он должен найти описание картинки (которое сгенерировал Vision API при загрузке) и ответить: *"На схеме изображены таблицы Users и Posts..."*.
* *Проверка:* Напиши `/sources`. В списке должен быть файл `03_media.md` или сама картинка.

**Тест 3.2: Аудио-память**

* *Ввод:* "О чем говорилось в записи встречи?"
* *Ожидание:* Ответ по содержанию транскрипции mp3 файла.

**Тест 3.3: Память диалога (History)**

* *Ввод:* "Как создать функцию в Python?"
* *Ответ:* (Показывает код).
* *Ввод:* "А как добавить в неё аргументы?" (Без уточнения, куда "в неё").
* *Ожидание:* Он должен понять, что речь про функцию из прошлого ответа.

**Тест 3.4: Режим "Full Docs" (Глубокое чтение)**

* *Ввод:* `/quit` (перезапуск с флагом).

    ```powershell
    semantic chat --full-docs
    ```

* *Ввод:* "Сделай саммари документа 02\_structure.md".
* *Ожидание:* Он должен прочитать весь документ целиком (а не кусочками) и выдать связное резюме.

-----

## 🧪 Сценарий 4: Управление и Очереди (Ops)

**Тест 4.1: Очередь**

```powershell
semantic queue status
```

* **👀 Проверка:** Должна быть красивая табличка. Если всё обработано — нули или "Completed".

**Тест 4.2: Воркер (Run Once)**

```powershell
semantic worker run-once
```

* **👀 Проверка:** Должен написать "Processed X tasks" или "No tasks". Не должен упасть с ошибкой.

-----

### ✅ Чек-лист для тебя (копируй и отмечай)

* [ ] **Ingest:** Текст, MD и Медиа загрузились без ошибок.
* [ ] **Search:** Вектор ищет по смыслу.
* [ ] **Search:** FTS ищет по точному слову.
* [ ] **Search:** Медиа ищется по описанию (картинка) и транскрипции (аудио).
* [ ] **Chat:** Отвечает на вопросы по документам.
* [ ] **Chat:** Видит содержимое картинок и аудио.
* [ ] **Chat:** Помнит контекст ("А что насчет этого?").
* [ ] **Chat:** Команда `/sources` показывает правильные файлы.

Если ты пройдешь этот чек-лист и везде будет "Да" — **система готова к интеграции во Flask**. Ты будешь уверен, что бэкенд (CLI/Core) железобетонный.
