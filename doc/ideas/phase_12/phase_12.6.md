# Phase 12.6: Search — Similarity Score Architecture Fix

**Статус:** 📋 СПЕЦИФИКАЦИЯ  
**Дата:** 2025-12-06 (расширено после аудита)  
**Зависимость:** Phase 12.5  
**Цель:** Исправить систему отображения score + добавить threshold slider

---

## 🐛 Контекст: Обнаруженная проблема

При поиске по запросу **"санта"** пользователь увидел:
- "Поздравления санты" — **49%**
- "Домашняя работа по вёрстке" — **45%**

**Вопрос:** Почему "домашка по вёрстке" имеет 45% схожести с "санта"?

**Ответ после аудита:** Это НЕ баг семантики, а **баг отображения процентов**.

---

## 🔍 Архитектурный аудит: Цепочка вычисления Score

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        ПОЛНАЯ ЦЕПОЧКА ДАННЫХ                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. SQLite-vec: vec_distance_cosine()                                       │
│     └── Возвращает DISTANCE: 0.0 = идентичны, 2.0 = противоположны          │
│                                                                             │
│  2. adapter.py: конвертация distance → score                                │
│     └── ⚠️ ДВЕ РАЗНЫЕ ФОРМУЛЫ (см. Проблема #1)                             │
│                                                                             │
│  3. ChunkResult/SearchResult DTO                                            │
│     └── score хранится как float, но диапазон зависит от метода!            │
│                                                                             │
│  4. search_service.py: _normalize_rrf_score()                               │
│     └── ⚠️ Применяется КО ВСЕМ режимам (см. Проблема #2)                    │
│                                                                             │
│  5. UI: {{ result.score_percent }}%                                         │
│     └── Пользователь видит "45%" или "49%"                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 🔴 Проблема #1: Несогласованные формулы конвертации

В ядре используются **ДВЕ РАЗНЫЕ формулы** для конвертации distance в score:

| Метод | Файл:строка | Формула | Диапазон score |
|-------|-------------|---------|----------------|
| `_vector_search` (документы) | `adapter.py:435` | `score = 1.0 - distance` | 0.0 – 1.0 |
| `_vector_search_chunks` | `adapter.py:935` | `score = 1.0 / (1.0 + distance)` | 0.5 – 1.0 |
| Hybrid RRF | `adapter.py:618-621` | `1/(k+rank_v) + 1/(k+rank_f)` | 0.01 – 0.033 |

### Почему это проблема?

- **Линейная формула** `1.0 - distance`: интуитивная, 0% = не похоже, 100% = идентично
- **Нелинейная формула** `1.0 / (1.0 + distance)`: сжимает диапазон к 0.5–1.0
- **RRF score**: показывает ПОЗИЦИЮ в рейтинге, не абсолютную схожесть

При distance = 0.5:
- Линейная: score = 0.5 (50%)
- Нелинейная: score = 0.667 (67%)

---

## 🔴 Проблема #2: Универсальная нормализация для всех режимов

Файл `search_service.py:113-127`:

```python
def _normalize_rrf_score(score: float, max_score: float = 0.033) -> int:
    """RRF score обычно в диапазоне 0.01-0.033 (для k=60)."""
    normalized = min(score / max_score, 1.0)
    return int(normalized * 100)
```

Эта функция **ВСЕГДА** вызывается в `_chunk_result_to_item()`:

```python
score_percent=_normalize_rrf_score(result.score),  # ← Для ВСЕХ режимов!
```

### Таблица: Как искажаются проценты

| Режим | Raw Score | Формула нормализации | Результат |
|-------|-----------|---------------------|-----------|
| Vector (chunks) | 0.833 | 0.833 / 0.033 = 25.2 → clamp | **100%** ❌ |
| Vector (chunks) | 0.667 | 0.667 / 0.033 = 20.2 → clamp | **100%** ❌ |
| Hybrid RRF | 0.0164 | 0.0164 / 0.033 = 0.49 | **49%** ✓ |
| Hybrid RRF | 0.0148 | 0.0148 / 0.033 = 0.45 | **45%** ✓ |

**Вывод:** Проценты 49% и 45% означают, что использовался **hybrid режим** с реальными RRF scores.

---

## 🔴 Проблема #3: RRF Score ≠ Семантическая близость

RRF (Reciprocal Rank Fusion) объединяет **РАНГИ**, а не расстояния:

```sql
(1.0 / (60 + rank_vector) + 1.0 / (60 + rank_fts)) as rrf_score
```

**Пример для запроса "санта":**

| Документ | Vector Distance | Vector Rank | RRF Score | Percent |
|----------|-----------------|-------------|-----------|---------|
| "Санта" | 0.3 | 1 | 1/61 ≈ 0.0164 | 49% |
| "Домашка" | 0.9 | 2 | 1/62 ≈ 0.0161 | 45% |

**Критичный момент:** Даже если "домашка" имеет distance = 0.9 (почти не похоже!), она всё равно будет #2 в рейтинге, **если других документов нет или они ещё хуже**.

RRF показывает: "Это второй лучший результат" — но НЕ показывает: "Насколько он вообще релевантен?"

---

## 🔴 Проблема #4: Отсутствие threshold фильтрации

В коде нет проверки минимальной релевантности:

```python
# Нигде в adapter.py нет:
if distance > 0.7:  # Слишком нерелевантно
    continue
```

Возвращаются ВСЕ результаты до limit, даже полностью нерелевантные.

---

## 🎯 План исправлений

### Часть A: Исправления в ядре (semantic_core)

#### A.1 Унифицировать формулу конвертации

**Файл:** `semantic_core/infrastructure/storage/peewee/adapter.py`

- Строка 435: уже `score = 1.0 - distance` ✓
- Строка 935: заменить `1.0 / (1.0 + distance)` → `1.0 - distance`

**Решение по выбору формулы:**
- Использовать `score = max(0.0, 1.0 - distance)` везде
- Линейная формула интуитивна: 100% = идентично, 0% = противоположно

#### A.2 Добавить поле distance в DTO (опционально)

**Файл:** `semantic_core/domain/models.py`

Добавить в `ChunkResult` и `SearchResult`:
```python
distance: Optional[float] = None  # Raw distance для отладки
```

Это позволит UI показывать реальное расстояние независимо от режима.

### Часть B: Исправления во Flask App

#### B.1 Mode-aware нормализация

**Файл:** `app/services/search_service.py`

Заменить `_normalize_rrf_score()` на умную функцию:

```python
def _normalize_score(score: float, match_type: MatchType) -> int:
    """Нормализовать score в проценты с учётом режима поиска."""
    if match_type == MatchType.HYBRID:
        # RRF score: 0.01-0.033 → 0-100%
        return int(min(score / 0.033, 1.0) * 100)
    else:
        # Vector/FTS score: 0.0-1.0 → 0-100%
        return int(score * 100)
```

#### B.2 Threshold Slider (оригинальная задача)

Добавить UI-контрол для фильтрации по минимальному score.

**Важно:** После фикса A.1 slider будет работать корректно, т.к. все режимы будут возвращать score в диапазоне 0.0–1.0.

---

## 🔧 Детальные задачи

### Задача 1: Фикс формулы в adapter.py

**Файл:** `semantic_core/infrastructure/storage/peewee/adapter.py`
**Строка:** 935

Заменить:
```python
score = 1.0 / (1.0 + distance)
```

На:
```python
score = max(0.0, 1.0 - distance)
```

### Задача 2: Mode-aware нормализация в Flask

**Файл:** `app/services/search_service.py`

1. Удалить функцию `_normalize_rrf_score()`
2. Создать функцию `_normalize_score(score, match_type)`
3. Обновить `_chunk_result_to_item()` и `_search_result_to_item()`

### Задача 3: Обновить CSS-классы score

**Файл:** `app/services/search_service.py`

Функция `_score_to_class()` использует пороги для RRF (0.02, 0.01).  
После фикса нужны пороги для 0.0–1.0:

```python
def _score_to_class(score: float, match_type: MatchType) -> str:
    if match_type == MatchType.HYBRID:
        # RRF пороги (старые)
        if score >= 0.02: return "score-high"
        elif score >= 0.01: return "score-medium"
        return "score-low"
    else:
        # Vector/FTS пороги (новые)
        if score >= 0.7: return "score-high"
        elif score >= 0.4: return "score-medium"
        return "score-low"
```

### Задача 4: UI — Threshold Slider

**Файл:** `app/templates/search.html`

Добавить range slider для фильтрации:
- Диапазон: 0% — 100%
- Default: 0% (показывать всё)
- Step: 5%

### Задача 5: Backend — применить threshold

**Файлы:** `app/routes/search.py`, `app/services/search_service.py`

1. Route читает `min_score` из request
2. Service фильтрует результаты: `if result.score >= min_score`

### Задача 6 (опционально): Добавить distance в DTO

**Файл:** `semantic_core/domain/models.py`

Поле `distance: Optional[float]` для отладки и продвинутого UI.

---

## 💡 Архитектурные решения

### Решение 1: Где хранить threshold?

**Вариант A:** Только в Flask (фильтрация после поиска)
- ✅ Проще, не трогаем ядро
- ❌ Загружаем лишние данные

**Вариант B:** В ядре (SQL WHERE)
- ✅ Эффективнее
- ❌ Требует изменений интерфейса VectorStore

**Выбор для MVP:** Вариант A — фильтрация в `SearchService`

### Решение 2: Что показывать для RRF?

RRF score не показывает абсолютную релевантность. Варианты:

**A)** Показывать RRF% как сейчас (после фикса нормализации)
**B)** Показывать "Ранг: #1, #2, #3" вместо процентов
**C)** Для RRF подгружать distance лучшего чанка и показывать его

**Выбор для MVP:** Вариант A — проще, пользователи привыкли к %

### Решение 3: Какой threshold по умолчанию?

- `0%` — показывать всё (текущее поведение)
- `30%` — отсечь явный мусор
- Настраиваемый в конфиге

**Выбор:** 0% по умолчанию, пользователь сам двигает slider

---

## 🧪 Тесты

### Unit-тесты для нормализации

```python
def test_normalize_score_vector():
    assert _normalize_score(1.0, MatchType.VECTOR) == 100
    assert _normalize_score(0.5, MatchType.VECTOR) == 50
    assert _normalize_score(0.0, MatchType.VECTOR) == 0

def test_normalize_score_hybrid():
    assert _normalize_score(0.033, MatchType.HYBRID) == 100
    assert _normalize_score(0.0165, MatchType.HYBRID) == 50
    assert _normalize_score(0.0, MatchType.HYBRID) == 0
```

### Integration-тесты для threshold

```python
def test_search_min_score_filters_results(client):
    # Без фильтра — больше результатов
    response = client.get("/search/results?q=python&min_score=0")
    count_all = response.data.count(b"result-card")
    
    # С высоким порогом — меньше результатов
    response = client.get("/search/results?q=python&min_score=70")
    count_filtered = response.data.count(b"result-card")
    
    assert count_filtered <= count_all
```

### E2E тест: проверка процентов

```python
def test_vector_search_shows_correct_percentage(client):
    """После фикса vector search должен показывать реальный %."""
    response = client.get("/search/results?q=python&mode=vector")
    # Парсим результаты, проверяем что % в диапазоне 0-100
    # и не все равны 100%
```

---

## 📊 Чеклист

### Ядро (semantic_core)
- [ ] Унифицировать формулу score в `adapter.py:935`
- [ ] (Опционально) Добавить поле `distance` в DTO

### Flask App
- [ ] Создать `_normalize_score(score, match_type)`
- [ ] Обновить `_score_to_class()` для разных режимов
- [ ] Range slider в `search.html`
- [ ] JavaScript для отображения значения slider
- [ ] HTMX include обновлён
- [ ] Route обрабатывает `min_score`
- [ ] Service фильтрует результаты

### Тесты
- [ ] Unit-тесты для `_normalize_score()`
- [ ] Integration-тесты для threshold filter
- [ ] E2E тест на корректность процентов

---

## 📚 Связанные материалы

- `doc/architecture/04_search_types.md` — типы поиска
- `doc/architecture/05_hybrid_search_rrf.md` — RRF алгоритм
- `semantic_core/infrastructure/storage/peewee/adapter.py` — реализация поиска
