[Image of software testing pyramid]

Принято. Это критически важный этап. Внедрение асинхронности (Async) и пакетной обработки (Batching) радикально меняет поведение системы. Раньше метод `ingest()` гарантировал результат, теперь он гарантирует только *обещание* результата. Тесты должны научиться работать с этим "отложенным ожиданием".

Плюс, ты правильно заметил: нам нужны **реальные E2E тесты** с Google Batch API, чтобы убедиться, что мы правильно формируем JSONL и Google нас понимает.

Вот детальный план **Фазы 5.1: Асинхронное и Интеграционное Тестирование**.

-----

### 🗺️ Цели Фазы 5.1

1. **Валидация Асинхронности:** Доказать, что при `mode='async'` данные попадают в БД со статусом `PENDING` и не блокируют выполнение.
2. **Проверка State Machine:** Убедиться, что переходы `PENDING` -\> `SUBMITTED` -\> `PROCESSING` -\> `COMPLETED` работают корректно и обратимы при сбоях.
3. **Real-World Batching:** Прогнать реальный цикл на Google Batch API с маленьким пакетом (2-3 чанка), чтобы проверить формат JSONL, права доступа и парсинг ответа.
4. **Регрессия Синхронности:** Убедиться, что старый режим `mode='sync'` все еще работает для одиночных задач.

-----

### 📂 Обновление Структуры Тестов

Мы выделяем тесты асинхронности в отдельную категорию и добавляем слой E2E тестов.

```text
tests/
├── conftest.py                  # Обновленные фикстуры (BatchClient, Async DB)
├── unit/
│   ├── infrastructure/
│   │   └── batching/
│   │       └── test_jsonl_builder.py  # Проверка формата JSONL (строгая)
│   ├── core/
│   │   └── test_batch_manager.py      # Логика группировки и смены статусов (Mock)
│   └── ...
├── integration/                 # Тесты с БД (но Mock API)
│   ├── pipeline/
│   │   └── test_async_ingestion.py    # Ingest -> DB Check (Pending)
│   └── worker/
│       └── test_worker_lifecycle.py   # Эмуляция полного цикла обработки
├── e2e/                         # <--- НОВОЕ: Реальные вызовы API
│   └── google_batch/
│       └── test_real_batch_flow.py    # Тот самый тест с 2-3 чанками
```

-----

### 🛠️ Детальный план работ

#### 1\. Обновление Инфраструктуры (`conftest.py`)

* **Фикстура `batch_db`:** База данных с уже созданными таблицами `BatchJob` и `ProcessingQueue`.
* **Фикстура `mock_batch_client`:** Имитирует Google API.
  * `submit_job` -\> возвращает `mock-job-id`.
  * `get_status` -\> возвращает `SUCCEEDED` через 1 секунду.
  * `get_results` -\> возвращает фейковые вектора.
* **Маркер `@pytest.mark.real_api`:** Специальный декоратор для E2E тестов, чтобы они не запускались случайно (это деньги и время).

#### 2\. Unit-тесты: Логика и Форматы

Здесь мы ловим ошибки формирования данных до отправки.

* **`test_jsonl_builder.py`:**
  * Создаем 3 чанка.
  * Генерируем JSONL.
  * *Assert:* Проверяем, что каждая строка — валидный JSON.
  * *Assert:* Проверяем структуру Google Request (`taskType`, `model`, `content`). Ошибка в одной букве здесь сломает батч.
* **`test_batch_manager_grouping`:**
  * Создаем 100 `PENDING` чанков.
  * Вызываем `flush_queue(batch_size=30)`.
  * *Assert:* Создалось 4 `BatchJob` (30+30+30+10). Статусы чанков обновились.

#### 3\. Интеграционные Тесты: Жизненный Цикл (Mock)

Проверяем, как компоненты взаимодействуют через БД.

* **Сценарий "Успешная обработка":**

    1. `pipeline.ingest(doc, mode='async')`.
    2. Проверяем БД: Чанки есть, вектора нет, статус `PENDING`.
    3. `batch_manager.flush_queue()`.
    4. Проверяем БД: Появился `BatchJob` (`SUBMITTED`), статус чанков `PENDING` (или `PROCESSING`), есть `batch_job_id`.
    5. Эмулируем ответ Гугла (Mock).
    6. `batch_manager.sync()`.
    7. Проверяем БД: `BatchJob` -\> `COMPLETED`, Чанки -\> `READY`, Вектора заполнены.

* **Сценарий "Сбой и Восстановление":**

    1. Отправляем батч. Статус `SUBMITTED`.
    2. Эмулируем ошибку API при проверке статуса (Network Error).
    3. Повторный вызов `sync()`.
    4. *Assert:* Батч не "потерялся", не перешел в `FAILED` навечно, система пытается проверить его снова.

#### 4\. E2E Тесты: Реальный Google Batch API (`test_real_batch_flow`)

Самая "мякоть". Этот тест будет идти долго (минуты), но он гарантирует, что интеграция работает.

**Алгоритм теста:**

1. **Setup:** Инициализируем реальный `GeminiBatchClient` с ключом.
2. **Ingest:** Создаем 3 синтетических документа ("Test Doc 1", "Test Doc 2"...). Загружаем их в БД (`async`).
3. **Flush:** Вызываем `flush_queue()` (принудительно, игнорируя лимиты на мин. размер).
4. **Wait Loop (Polling):**
      * Запускаем цикл `while` с `sleep(10)`.
      * Каждые 10 секунд вызываем `check_and_sync()`.
      * Печатаем статус в консоль.
      * Таймаут: 10 минут (Google Batch обычно быстрее, но с запасом).
5. **Verify:**
      * Когда статус станет `COMPLETED`:
      * Проверяем, что в БД у этих 3 чанков появились вектора.
      * Делаем пробный `search("Test")`.
      * *Assert:* Документы найдены.
6. **Cleanup:** Удаляем тестовые записи из БД.

#### 5\. Регрессионные Тесты

* **Sync Mode:** Проверяем, что `pipeline.ingest(doc, mode='sync')` всё еще работает синхронно и возвращает результат сразу.
* **Hybrid Search:** Проверяем, что поиск корректно игнорирует чанки со статусом `PENDING` (или наоборот, предупреждает пользователя).

### ✅ Итог Фазы 5.1

После этого этапа у тебя будет:

1. **Уверенность:** Ты точно знаешь, что библиотека умеет "общаться" с Google Batch API правильно.
2. **Безопасность:** Логика очередей покрыта тестами на сбои.
3. **Инструмент отладки:** E2E тест можно использовать как скрипт диагностики (`run_diagnostic.py`), если в продакшене возникнут проблемы с API.

**Готов приступить к реализации E2E теста и моков?**
