Это абсолютно точное замечание. Поскольку мы перешли от "набора скриптов" к **Дескрипторам (`SemanticIndex`)** и автоматической магии, старые тесты, проверяющие `save_note_with_chunks`, бесполезны. Нам нужно тестировать **Интеграцию** и **Поведение**.

Вот обновленный план **Фазы 3.1**, заточенный под новую архитектуру.

-----

### 🗺️ План Фазы 3.1: Тестирование Интеграции и Магии

Поскольку библиотека теперь "живет" внутри моделей пользователя, тесты должны эмулировать поведение разработчика, использующего библиотеку. Мы будем создавать временные модели Peewee "на лету" и проверять, срабатывает ли наша магия.

#### 📂 Структура тестов

Мы четко разделяем проверку "чистой логики" и проверку "магии ORM".

```text
tests/
├── conftest.py                  # Фикстуры (MockEmbedder, TempDB, DynamicModels)
├── unit/                        # ⚡ ЧИСТАЯ ЛОГИКА (DTO, Builders)
│   ├── processing/
│   │   └── test_simple_splitter.py
│   ├── integrations/
│   │   └── test_document_builder.py  # Проверка сборки DTO из полей модели
│   └── domain/
│       └── ...
├── integration/                 # 🐢 МАГИЯ ORM (Peewee + Signals)
│   ├── descriptor/
│   │   ├── test_registration.py      # Создаются ли таблицы _vec/_fts?
│   │   └── test_signals.py           # Вызывается ли пайплайн при .save()?
│   ├── search/
│   │   └── test_proxy_methods.py     # Работает ли Model.search.hybrid()?
│   └── ...
```

-----

### 🛠️ Детальный план работ

#### 1\. Обновление Инфраструктуры (`conftest.py`)

Нам нужны фикстуры, которые создают "песочницу" для каждой тестовой функции.

* **Фикстура `mock_embedder`:** Тот же фейковый генератор векторов (детерминированный рандом), чтобы тесты были быстрыми и бесплатными.
* **Фикстура `test_db`:** In-memory SQLite база данных с загруженным `sqlite-vec`.
* **Фикстура `create_model`:** Хелпер, который позволяет определить класс модели Peewee прямо внутри теста:

    ```python
    # Пример использования в тесте
    Model = create_model(fields={'text': TextField()}, index_config={'source': ['text']})
    ```

#### 2\. Unit-тесты: Строители и Конфигурация

Проверяем компоненты, которые не зависят от базы данных.

* **`test_document_builder.py`:**
  * Создаем фейковый объект (Mock object) с полями `title="A", body="B"`.
  * Конфигурируем `DocumentBuilder(content="body", context=["title"])`.
  * *Assert:* Проверяем, что на выходе `Document`, где `content="B"` и в метаданных лежит `title="A"`.
* **`test_descriptor_config.py`:**
  * Проверяем валидацию параметров `SemanticIndex` (например, что нельзя указать несуществующее поле).

#### 3\. Интеграционные Тесты: Жизненный цикл Дескриптора

Самая важная часть. Проверяем, работает ли "Магия".

* **Тест Регистрации (`test_registration.py`):**
  * Определяем модель с `search = SemanticIndex(...)`.
  * Инициализируем БД.
  * *Assert:* Проверяем через `db.get_tables()`, что автоматически создались таблицы `model_search_vec` и `model_search_fts`.
* **Тест Перехвата (`test_signals.py`):**
  * Создаем инстанс модели: `obj = Model(text="Hello")`.
  * Вызываем `obj.save()`.
  * *Assert:* Проверяем (через Mock пайплайна или прямым запросом в `_vec` таблицу), что вектор действительно был сгенерирован и сохранен.
* **Тест Поиска (`test_proxy_methods.py`):**
  * Сохраняем 3 объекта.
  * Вызываем `Model.search.vector("query")`.
  * *Assert:* Проверяем, что метод возвращает список объектов модели (а не ID или кортежи).
  * *Assert:* Проверяем, что к объектам динамически добавилось поле `distance` или `score`.

#### 4\. Регрессия и Граничные Случаи

* **Обновление:** Что будет, если изменить текст и снова вызвать `.save()`? (Должен обновиться вектор).
* **Удаление:** Что будет, если вызвать `.delete_instance()`? (Вектор должен удалиться).
* **Пустые поля:** Что если сохранить объект с пустым текстом? (Пайплайн не должен падать).

### ✅ Итог Фазы 3.1

После этого этапа мы будем уверены, что наш новый архитектурный подход (Дескрипторы) не просто красив на бумаге, но и реально работает с базой данных и сигналами Peewee.

Это даст зеленый свет для реализации сложных парсеров (Фаза 4) и очередей (Фаза 5), так как мы будем знать, что фундамент (интеграция) надежен.
