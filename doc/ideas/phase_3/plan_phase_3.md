Это великолепный план. Мы переходим от "набора инструментов" к **"Экосистеме Интеграции"**. Мы прячем сложность пайплайнов за элегантным фасадом дескрипторов Python.

Ниже детальный план **Фазы 3: Универсальная Интеграция и Декларативный API**.

-----

### 🗺️ Концепция: "Magic Proxy" (Магический Прокси)

Вместо того чтобы заставлять пользователя писать код для обработки данных, мы даем ему **Дескриптор (`SemanticIndex`)**. Этот объект живет внутри модели и выступает мостом между "Миром ORM" и "Миром Векторов".

* **На входе (Save):** Он работает как пылесос — вытягивает данные из указанных полей модели, собирает их в `Document` и отправляет в пайплайн.
* **На выходе (Search):** Он работает как прокси — принимает запрос, ищет ID через ядро, а затем делает выборку из родной таблицы модели.

-----

### 📦 1. Структура Пакета (`semantic_core.integrations`)

Мы создаем новый слой, который изолирует ядро от конкретных ORM.

```text
semantic_core/
├── integrations/
│   ├── __init__.py           # Экспорт SemanticIndex
│   ├── base.py               # Логика Дескриптора и DocumentBuilder
│   ├── search_proxy.py       # Интерфейс поиска (Hybrid/Vector/FTS)
│   └── peewee/               # Адаптер для Peewee
│       ├── __init__.py
│       ├── adapter.py        # Реализация подписки на сигналы
│       └── utils.py          # Хелперы для выборки по ID
```

-----

### ⚙️ 2. Механизм Дескриптора (`SemanticIndex`)

Это главный класс, который пользователь видит в своем коде. Он реализует паттерн **Descriptor Protocol**.

#### А. Конфигурация (В конструкторе)

Принимает правила сборки документа ("Комбо-поля"):

* `content_field`: Основное поле для нарезки и поиска (Body).
* `context_fields`: Список полей для "запекания" в вектор (Title, Category).
* `media_fields`: Список полей с путями к картинкам/видео.
* `filter_fields`: Поля, по которым будут доступны жесткие SQL-фильтры.

#### Б. Магия `__set_name__` (Регистрация)

* Когда класс модели создается, дескриптор узнает свое имя и модель-владельца.
* Вызывает `ORMAdapter.register_hooks()`, чтобы подписаться на события `post_save` и `pre_delete`.
* Инициирует проверку схемы: если виртуальных таблиц нет, вызывает их создание (Lazy Provisioning).

#### В. Магия `__get__` (Доступ)

* **Class Access (`Model.search`):** Возвращает объект `SearchProxy`. Это точка входа в поиск.
* **Instance Access (`obj.search`):** Возвращает объект `InstanceManager`. Позволяет принудительно обновить индекс для конкретной записи: `user.search.update()`.

-----

### 🏗️ 3. Строитель Документа (`DocumentBuilder`)

Этот компонент отвечает за превращение "сырой" модели в наш DTO `Document`.

**Логика работы:**

1. **Extraction:** Извлекает данные из атрибутов модели по именам полей, указанным в конфиге.
2. **Composition:**
      * Текст из `content_field` становится телом документа.
      * Данные из `context_fields` собираются в словарь `metadata`.
      * Данные из `media_fields` превращаются в список объектов `MediaResource`.
3. **Output:** Возвращает готовый `Document`, который идеально подходит для входа в `IngestionPipeline`.

-----

### 🔎 4. Прокси Поиска (`SearchProxy`)

Этот объект возвращается, когда пользователь пишет `Model.search`. Он предоставляет красивый API для всех типов запросов.

**Методы:**

* `hybrid(query, k=60, limit=10, **filters)`:

    1. Делегирует поиск ядру (`VectorStore`).
    2. Получает список `SearchResult` (где лежат ID и скоры).
    3. Вызывает `ORMAdapter.fetch_objects(ids)`, чтобы получить настоящие объекты модели.
    4. Склеивает объекты со скорами и возвращает результат.

    <!-- end list -->

  * **Важно:** Сохраняет сортировку RRF.

* `vector(query, limit=10, **filters)`:

  * Чистый семантический поиск. Полезно для рекомендаций ("похожие товары").

* `fts(query, limit=10, **filters)`:

  * Чистый текстовый поиск. Полезно для точных совпадений ("артикул 123").

-----

### 🔌 5. Адаптер Peewee (`PeeweeAdapter`)

Реализация "грязной" работы с конкретной ORM.

* **Хуки:** Использует `playhouse.signals` для перехвата сохранения.
* **Schema Management:**
  * Генерирует SQL для создания таблиц `_vec` и `_fts` на основе имени таблицы модели.
  * Создает триггеры для автоматической синхронизации FTS (так как FTS в SQLite лучше обновлять триггерами, чем кодом).
* **Fetching:** Делает эффективный запрос `SELECT * FROM table WHERE id IN (...)` с сохранением порядка (через `CASE WHEN` или сортировку в Python, так как SQL не гарантирует порядок `IN`).

-----

### 🔄 6. Поток Данных (Data Flow)

**Сценарий: Сохранение (Ingestion)**

1. User: `article.save()`
2. Peewee: Пишет в SQLite. Шлет сигнал.
3. Adapter: Ловит сигнал. Передает инстанс в Дескриптор.
4. Builder: Вытягивает заголовок, текст и картинку. Создает `Document`.
5. Core: Пайплайн режет, векторизует, сохраняет чанки в теневые таблицы.

**Сценарий: Поиск (Retrieval)**

1. User: `Article.search.hybrid("обзор", category="news")`
2. Proxy: Передает запрос в Core.
3. Core: Векторизует запрос. Делает RRF в теневых таблицах. Возвращает ID `[5, 1, 8]`.
4. Adapter: Делает `SELECT * FROM articles WHERE id IN (5, 1, 8)`.
5. Proxy: Возвращает список объектов `Article`.

-----

### ✅ Итог Фазы 3

Мы получаем библиотеку, которая **не навязывает** наследование.

* **Чистота:** Модели пользователя остаются чистыми.
* **Гибкость:** Можно иметь два индекса (`search_ru`, `search_en`) на одной модели.
* **Мощь:** Доступны все виды поиска (Hybrid, Vector, FTS) через единый интерфейс.
* **Будущее:** Когда мы захотим добавить SQLAlchemy, мы просто напишем `AlchemyAdapter`, а логика `SemanticIndex` и `DocumentBuilder` останется неизменной.

План утвержден. Можно приступать к реализации базовых классов интеграции.
