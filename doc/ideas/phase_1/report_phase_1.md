Это отличный, системный подход. Мы продолжаем строить наше ядро по принципам SOLID, и Фаза 2 посвящена "приземлению" наших абстракций на реальные технологии баз данных.

В Фазе 1 мы определили интерфейс `VectorStore`, который говорит системе: *"Мне нужно сохранить документ с чанками"* или *"Мне нужно найти похожие чанки"*. В Фазе 2 мы реализуем этот контракт, используя **SQLite** (через Peewee) и оставляем задел под **PostgreSQL** (через SQLAlchemy 2.0).

Ниже подробный план реализации **Фазы 2: Слой Хранения Данных**.

-----

### 📂 Структура Пакета Infrastructure/Storage

Мы расширяем папку `infrastructure` новыми модулями. Каждый модуль отвечает за конкретную технологию ORM.

```text
semantic_core/
├── infrastructure/
│   ├── storage/
│   │   ├── __init__.py           # Экспорт адаптеров
│   │   ├── base_models.py        # Общие миксины (если нужно)
│   │   ├── peewee/               # Реализация для Peewee (SQLite)
│   │   │   ├── __init__.py
│   │   │   ├── adapter.py        # Реализация VectorStoreInterface
│   │   │   ├── models.py         # Внутренние модели (Parent, Chunk)
│   │   │   └── engine.py         # Подключение и загрузка расширений
│   │   └── alchemy/              # Реализация для SQLAlchemy (Заглушка)
│   │       ├── __init__.py
│   │       ├── adapter.py        # Заглушка VectorStoreInterface
│   │       └── ...
```

-----

### 🧩 1. Проектирование Схемы Данных (Внутренние модели)

Важно понимать: ядро (`domain/document.py`) оперирует чистыми объектами DTO. Слой хранения должен преобразовать их в свои внутренние ORM-модели.

**Логика схемы Parent-Child (для Peewee и SQLAlchemy):**

1.  **Таблица `Documents` (Родители):**

      * **Назначение:** Хранит метаданные файла и исходный текст для "человеческого" чтения и FTS поиска.
      * **Поля:** `id` (Auto PK), `hash` (для дедупликации, чтобы не сохранять один файл дважды), `content` (текст), `meta_json` (словарь метаданных), `created_at`.
      * **Индексы:** FTS5 виртуальная таблица, связанная с этой таблицей триггерами.

2.  **Таблица `Chunks` (Дети):**

      * **Назначение:** Хранит нарезанные куски текста.
      * **Поля:** `id`, `document_id` (FK), `chunk_index` (порядок), `content` (текст чанка), `meta_json` (здесь будут лежать заголовки Markdown или таймкоды).

3.  **Таблица `Vectors` (Индекс):**

      * **Назначение:** Виртуальная таблица `vec0` (для SQLite) или `vector` (для PGVector).
      * **Связь:** `rowid` (или FK) -\> `Chunks.id`.
      * **Поля:** `embedding` (массив float).

-----

### ⚙️ 2. Реализация Адаптера Peewee (`infrastructure/storage/peewee`)

Это наш "рабочий лошадка" для локального использования.

#### А. Модуль `engine.py` (Движок)

  * **Класс:** `PeeweeEngine`
  * **Задача:** Инициализация соединения.
  * **Логика:**
      * Наследуется от `SqliteExtDatabase`.
      * В методе подключения (`_connect` или хук) обязательно вызывает загрузку C-расширения `sqlite-vec`.
      * Включает режим WAL (Write-Ahead Logging) для скорости.
      * Реализует функцию `create_schema()`, которая выполняет сырой SQL для создания таблиц `vec0` и `fts5`, если их нет. Это замена миграциям.

#### Б. Модуль `adapter.py` (Адаптер)

  * **Класс:** `PeeweeVectorStore`
  * **Наследует:** `VectorStoreInterface` (из Core).
  * **Метод `save(document: Document, chunks: List[Chunk])`:**
      * Открывает транзакцию (`atomic`).
      * Проверяет по хешу, есть ли такой документ. Если есть — обновляет (удаляет старые чанки).
      * Сохраняет `Document` -\> получает ID.
      * Массово (bulk) сохраняет `Chunks` с привязкой к ID документа.
      * Массово (через сырой SQL) вставляет вектора в таблицу `vec0`, используя упаковку `struct.pack` для бинарного формата.
  * **Метод `search(query_vec, filters)`:**
      * Строит сложный SQL запрос с JOIN-ами.
      * Использует CTE (Common Table Expressions) для реализации алгоритма RRF (гибридного ранжирования).
      * Преобразует результаты (строки БД) обратно в объекты `SearchResult` (из Core), чтобы внешний код не знал, что мы использовали Peewee.

-----

### 🔮 3. Заготовка для Адаптера SQLAlchemy (`infrastructure/storage/alchemy`)

Мы создаем структуру, но пока не пишем реализацию (YAGNI), оставляя "заглушки".

#### А. Модуль `adapter.py`

  * **Класс:** `AlchemyVectorStore`
  * **Наследует:** `VectorStoreInterface`.
  * **Логика:**
      * Все методы (`save`, `search`) пока выбрасывают исключение `NotImplementedError("Postgres support coming soon")`.
      * Это позволяет нам уже сейчас в конфигурации приложения написать переключатель: `if config.USE_POSTGRES: store = AlchemyVectorStore(...)`.

#### Б. Концепция миграции (на будущее)

  * В SQLAlchemy 2.0 мы будем использовать `Mapped` классы.
  * Вместо `sqlite-vec` там будет использоваться расширение `pgvector`.
  * Поскольку интерфейс `VectorStore` у нас общий, для остальной программы подмена будет прозрачной.

-----

### 🛡️ Преимущества такой архитектуры

1.  **Полная изоляция:** Твой парсер Markdown или мультимодальный процессор не знают, какая база данных используется. Они работают с DTO.
2.  **Легкость тестирования:** Ты можешь написать `MemoryVectorStore` (просто словарь в памяти) для тестов, чтобы не создавать файлы БД при каждом прогоне юнит-тестов.
3.  **Масштабируемость:** Если завтра ты захочешь перенести данные на сервер с Postgres, тебе нужно будет написать только код в папке `alchemy`, не трогая логику нарезки или работы с Gemini.

**Итог Фазы 2:** У нас есть надежный механизм сохранения и поиска, скрытый за интерфейсом. Мы можем сохранять документы с чанками и векторами одной командой `store.save()`.

Готов переходить к описанию **Фазы 3**, где мы соберем всё это в работающий пайплайн с обработкой текста?