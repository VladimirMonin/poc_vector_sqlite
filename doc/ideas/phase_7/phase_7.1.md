# 📋 Phase 7.1: Processing Layer Instrumentation

**Статус:** 🔲 Планируется  
**Зависимости:** Phase 7.0 (Logging Core) ✅

---

## ✅ ПРЕДВАРИТЕЛЬНО ВЫПОЛНЕНО

> **ВАЖНО для агента:** Следующие задачи УЖЕ ВЫПОЛНЕНЫ. НЕ нужно их делать повторно!

1. ✅ **EMOJI_MAP обновлён** — все паттерны для Phase 7.1 добавлены в `formatters.py`:
   - `context`, `hierarchy`, `hierarchical` → 🧬
   - `asset`, `assets`, `enricher`, `enrichers` → 🖼️

2. ✅ **Logging Core готов** — `get_logger()`, `bind()`, `trace()` работают

**Агент НЕ должен трогать файл `semantic_core/utils/logger/formatters.py`!**

---

## 🎯 Цель

Внедрить семантическое логирование в слой обработки данных (Processing Layer), чтобы получить **полную видимость** трансформаций: парсинг → разбиение → обогащение контекстом.

---

## 📦 Целевые модули

```
semantic_core/processing/
├── parsers/
│   └── markdown_parser.py      # MarkdownNodeParser
├── splitters/
│   └── smart_splitter.py       # SmartSplitter  
├── context/
│   └── hierarchical_strategy.py # HierarchicalContextStrategy
└── enrichers/
    └── markdown_assets.py      # MarkdownAssetEnricher
```

**Всего: 4 файла**

---

## 🔍 Детальный план по каждому модулю

### 1. `markdown_parser.py` — MarkdownNodeParser

**Эмодзи модуля:** 🧶 (паттерн `parser`)

**Точки логирования:**

| Метод | Уровень | Что логируем |
|-------|---------|--------------|
| `parse()` вход | DEBUG | Длина контента, наличие frontmatter |
| `parse()` выход | INFO | Количество найденных нод по типам |
| `_process_token()` | TRACE | Тип токена, принятое решение |
| `_build_header_hierarchy()` | DEBUG | Уровни заголовков, breadcrumbs |
| Exception handlers | ERROR | Полный контекст ошибки |

**Контекст для bind():**

- `doc_id` — если передан
- `content_hash` — первые 8 символов хеша для идентификации

**Примеры сообщений:**

```
🧶 Начало парсинга: 4532 символов, frontmatter=True
🧶 Токен: heading level=2, content="Установка"  
🧶 Парсинг завершён: headers=5, paragraphs=12, code_blocks=3
```

---

### 2. `smart_splitter.py` — SmartSplitter

**Эмодзи модуля:** ✂️ (паттерн `splitter`)

**Точки логирования:**

| Метод | Уровень | Что логируем |
|-------|---------|--------------|
| `split()` вход | DEBUG | Количество нод на входе, настройки (max_tokens, overlap) |
| `split()` выход | INFO | Количество чанков, статистика по типам |
| `_split_node()` | TRACE | Решение о разбиении: split/keep/merge |
| `_merge_small_nodes()` | DEBUG | Количество смерженных нод |
| `_calculate_tokens()` | TRACE | Подсчёт токенов для ноды |

**Контекст для bind():**

- `doc_id`
- `node_index` — порядковый номер ноды

**Примеры сообщений:**

```
✂️ Разбиение 15 нод: max_tokens=800, overlap=100
✂️ Node[3] heading: 450 токенов, решение=KEEP
✂️ Node[7] paragraph: 1200 токенов, решение=SPLIT на 2
✂️ Разбиение завершено: 15 нод → 23 чанка
```

---

### 3. `hierarchical_strategy.py` — HierarchicalContextStrategy

**Эмодзи модуля:** 🧬 (добавить в EMOJI_MAP паттерн `context`, `hierarchy`)

**Точки логирования:**

| Метод | Уровень | Что логируем |
|-------|---------|--------------|
| `enrich()` вход | DEBUG | Количество чанков |
| `enrich()` выход | INFO | Сколько чанков обогащено |
| `_build_breadcrumbs()` | TRACE | Построенный путь (h1 > h2 > h3) |
| `_add_context_to_chunk()` | DEBUG | Размер добавленного контекста |

**Контекст для bind():**

- `doc_id`
- `chunk_id`

**Примеры сообщений:**

```
🧬 Обогащение 23 чанков иерархическим контекстом
🧬 Chunk[5]: breadcrumbs="Гайд > Установка > Linux"
🧬 Обогащение завершено: 23 чанка, avg контекст +45 токенов
```

---

### 4. `markdown_assets.py` — MarkdownAssetEnricher

**Эмодзи модуля:** 🖼️ (паттерн `asset`, `enricher`)

**Точки логирования:**

| Метод | Уровень | Что логируем |
|-------|---------|--------------|
| `enrich()` вход | DEBUG | Количество чанков, base_path |
| `_find_assets()` | DEBUG | Найденные ассеты по типам |
| `_resolve_path()` | TRACE | Резолв пути: relative → absolute |
| `_extract_alt_text()` | TRACE | Извлечённый alt-text |
| `enrich()` выход | INFO | Статистика: images, audio, video |

**Контекст для bind():**

- `doc_id`
- `asset_path`

**Примеры сообщений:**

```
🖼️ Сканирование 23 чанков на медиа-ассеты, base_path=/docs
🖼️ Найдено: images=3, audio=1, video=0
🖼️ Резолв пути: ./img/logo.png → /docs/img/logo.png
```

---

## 📐 Паттерн инструментирования

**Стандартный шаблон для каждого модуля:**

```python
# В начале файла (после импортов)
from semantic_core.utils.logger import get_logger

logger = get_logger(__name__)
```

**Паттерн для методов:**

1. **Вход:** `logger.debug()` с параметрами
2. **Ключевые решения:** `logger.trace()` с деталями
3. **Выход:** `logger.info()` с результатами
4. **Ошибки:** `logger.error_with_context()`

**Паттерн bind() для вложенных вызовов:**

```python
def process(self, doc_id: str, ...):
    log = logger.bind(doc_id=doc_id)
    log.debug("Starting process")
    
    for i, chunk in enumerate(chunks):
        chunk_log = log.bind(chunk_index=i)
        chunk_log.trace("Processing chunk")
```

---

## ✅ Acceptance Criteria

### Функциональные

1. [ ] Все 4 модуля инструментированы
2. [ ] Каждый публичный метод имеет DEBUG на входе и INFO/DEBUG на выходе
3. [ ] TRACE логи содержат детальные данные трансформаций
4. [ ] Ошибки логируются через `error_with_context()`
5. ~~EMOJI_MAP расширен новыми паттернами~~ ✅ УЖЕ СДЕЛАНО

### Качество

6. [ ] Логи не дублируют информацию
7. [ ] Контекст (doc_id, chunk_id) пробрасывается через bind()
8. [ ] Нет логирования чувствительных данных (контент пользователя — только длина/хеш)

### Тесты

9. [ ] Существующие тесты проходят
10. [ ] Добавлены тесты на наличие логгера в модулях (опционально)

---

## 🔧 Инструкции для агента-исполнителя

### КРИТИЧЕСКИ ВАЖНО: Полная обработка файлов

**Агент ОБЯЗАН:**

1. **Прочитать каждый файл ЦЕЛИКОМ** через `read_file` без offset/limit
2. **Найти ВСЕ методы и функции** — публичные и приватные
3. **Найти ВСЕ точки выброса исключений** — использовать терминал:

   ```bash
   grep -n "raise\|except\|Exception" semantic_core/processing/**/*.py
   ```

4. **Проверить ВСЕ return statements** — каждый выход из метода
5. **После инструментирования** — снова прочитать файл и убедиться, что ничего не пропущено

### Порядок работы

1. ~~Обновить `EMOJI_MAP` в `formatters.py`~~ ✅ УЖЕ СДЕЛАНО
2. Инструментировать `markdown_parser.py`
3. Инструментировать `smart_splitter.py`
4. Инструментировать `hierarchical_strategy.py`
5. Инструментировать `markdown_assets.py`
6. Запустить тесты: `pytest tests/unit/processing/ -v`
7. Проверить отсутствие регрессий

### Чеклист для каждого файла

- [ ] Добавлен импорт логгера
- [ ] Создан `logger = get_logger(__name__)`
- [ ] Все публичные методы имеют логи входа/выхода
- [ ] Все `raise` обёрнуты в логирование
- [ ] Все `except` блоки логируют ошибку
- [ ] bind() используется для вложенного контекста
- [ ] Файл перечитан после изменений для проверки

---

## 📊 Ожидаемый результат

После Phase 7.1 при обработке документа в логах будет видно:

```
🧶 [doc-123] Начало парсинга: 4532 символов
🧶 [doc-123] Парсинг завершён: headers=5, paragraphs=12
✂️ [doc-123] Разбиение 17 нод: max_tokens=800
✂️ [doc-123] Разбиение завершено: 17 нод → 23 чанка
🧬 [doc-123] Обогащение 23 чанков
🧬 [doc-123/chunk-5] breadcrumbs="Гайд > Установка"
🖼️ [doc-123] Найдено ассетов: images=3
```

---

## 🔗 Связанные документы

- **Предыдущая:** [Phase 7.0 — Logging Core](phase_7.0.md)
- **Следующая:** [Phase 7.2 — Infrastructure Layer](phase_7.2.md)
- **Архитектура:** [Semantic Logging](../../architecture/35_semantic_logging.md)
