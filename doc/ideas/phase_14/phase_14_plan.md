Владимир, это отличный план. Мы превращаем «баг-фикс» в архитектурный прорыв.

Ниже представлен детальный план **Phase 14: Media Pipeline Evolution**. Мы идем от «тушения пожара» (потери данных) к построению гибкого фреймворка.

------

# 🚀 Phase 14: Media Pipeline Evolution & Architecture

**Главная цель:** Переход от модели «1 файл = 1 чанк» к модели «1 файл = Дерево семантических чанков», снятие ограничений на размер контента и внедрение гибкой пайплайн-архитектуры.

------

## 🔥 Phase 14.0: The Critical Fix (Остановка потери данных)

**Задача:** Срочно исправить «кастрацию» контента (лимит 8к токенов) и архитектурную ошибку сохранения всего в один чанк. Реализуем жестко, внутри текущих методов, без лишних абстракций, чтобы спасти данные.

### 1. Снятие лимитов модели (Configuration Fix)

- **Где:** `semantic_core/infrastructure/gemini/audio_analyzer.py` (и Video/Image).
- **Изменение:** Убираем хардкод `max_output_tokens=8192`.
- **Реализация:**
  - Внедряем чтение лимита из конфига модели (Gemini 1.5 Flash/Pro поддерживают до 8192 в output *по умолчанию*, но через API можно запрашивать больше, если модель позволяет, либо использовать батчинг на уровне промпта, но для начала ставим максимально разрешенный API лимит).
  - *Сигнатура:* `generation_config={"max_output_tokens": 65536}` (для моделей серии 1.5/2.0).

### 2. Внедрение Smart-Splitter в медиа-инжест (Logic Fix)

- **Где:** `semantic_core/pipeline.py` -> метод `_ingest_direct_media`.
- **Концепция:** Вместо сохранения сырого ответа Gemini в базу, мы создаем виртуальный документ и режем его.
- **Алгоритм:**
  1. Получаем JSON от Gemini: `{summary, transcription, keywords}`.
  2. Создаем список `chunks_to_save = []`.
  3. **Саммари:** Создаем чанк типа `AUDIO_REF` (как сейчас), но в `metadata` пишем `role="summary"`. Это «обложка» файла.
  4. **Транскрипция:**
     - Берем текст транскрипции (который теперь может быть огромным).
     - Создаем временный `Document(content=transcription)`.
     - Вызываем `self.splitter.split(temp_doc)`.
     - Получаем список чанков (TEXT).
     - ОБДУМАТЬ ВАРИАНТ ГДЕ МЫ АНАЛИЗИРУЕМ ВИДЕО ИЛИ СКРИНЫ С КОДОМ _ ЧТОБЫ ПОЛУЧАТЬ MD блоки кода?????
     - В каждом чанке проставляем `metadata`: `{"role": "transcript", "parent_media_id": doc.id}`.
  5. **Сохранение:** Метод `store.save()` должен уметь принимать список чанков и атомарно сохранять их.

### 3. Результат фазы 14.0

- ✅ Аудио на 1 час сохраняется полностью.
- ✅ Поиск находит конкретные фразы из середины лекции.
- ✅ В базе лежат чанки двух ролей: `summary` (для общего поиска) и `transcript` (для детального).

------

## 🏗️ Phase 14.1: The Pipeline Abstraction (Рефакторинг)

**Задача:** Вынести логику обработки из монолитного `pipeline.py` в отдельные классы-шаги. Подготовка к плагинной системе.

### 1. Интерфейс ProcessingStep

- Создание абстрактного базового класса в `semantic_core/processing/steps/base.py`.

- **Сигнатура:**

  Python

  ```
  class BaseProcessingStep(ABC):
      @property
      def step_name(self) -> str: ...
  
      def process(self, context: MediaContext) -> List[Chunk]: ...
  ```

### 2. Реализация стандартных шагов

- **`SummaryStep`:** Отвечает за генерацию краткого описания и метаданных (то, что раньше было первой частью `_ingest_direct_media`).
- **`TranscriptionStep`:** Отвечает за получение транскрипции и (важно!) её нарезку через `SmartSplitter`. Вся логика нарезки инкапсулируется здесь.

### 3. Pipeline Executor

- В `SemanticCore` заменяем жесткий код на итерацию по списку шагов.

- **Концепция:**

  Python

  ```
  # Внутри ingest()
  context = MediaContext(file_path=...)
  all_chunks = []
  for step in self.pipeline_steps:
      chunks = step.process(context)
      all_chunks.extend(chunks)
  self.store.save(doc, all_chunks)
  ```

------

## 🧩 Phase 14.2: Aggregation & Service Layer (Сборка данных)

**Задача:** Научить приложение «собирать» разрозненные чанки обратно в единое представление для UI.

### 1. DTO для отображения

- Создание `MediaFullView` (или `MediaDocumentDTO`).
- Поля: `summary`, `transcript_segments`, `keywords`, `timeline`.

### 2. Сервис агрегации (MediaService)

- Реализация метода `get_media_details(doc_id)`.
- **Логика:**
  - Делает `SELECT * FROM chunks WHERE document_id = ?`.
  - Итерируется по результатам.
  - Если `metadata['role'] == 'summary'` → заполняет шапку.
  - Если `metadata['role'] == 'transcript'` → добавляет в список сегментов транскрипции (сортируя по `chunk_index`).

### 3. Обновление UI (Flask)

- Новая страница `/media/<id>`.
- Отображение плеера.
- Отображение транскрипции в виде "полотна" текста.

------

## 🔌 Phase 14.3: User Flexibility (Кастомизация)

**Задача:** Дать пользователю возможность внедрять свои промпты и шаги без изменения кода ядра.

### 1. Конфигурируемые промпты

- Вынос системных промптов (для саммари, для транскрипции) в `semantic.toml` или в параметры конструктора шагов.
- Пользователь может переопределить: *"Сделай саммари в стиле пиратской байки"*.

### 2. Регистрация кастомных шагов

- API для добавления шага: `core.register_step(MyCustomSentimentStep())`.
- Пример: Пользователь хочет вытаскивать таймкоды рекламных интеграций. Он пишет свой класс, наследуется от `BaseProcessingStep`, пишет свой промпт, возвращает чанки с `role="ad_spot"`.

### 3. Механизм Re-run (Идемпотентность)

- Возможность запустить конкретный шаг для уже существующего документа.
- *Сценарий:* "Я поменял промпт для саммари, хочу перегенерировать только саммари, не трогая дорогую транскрипцию".
- Реализация через удаление старых чанков с конкретным `role` перед запуском шага.

------

### Итоговый результат Фазы 14

Мы получим систему, где:

1. **Нет потерь данных:** Длинные файлы обрабатываются полностью.
2. **Поиск точен:** Ищутся конкретные фрагменты речи, а не общие описания.
3. **Архитектура чиста:** Логика разбита на изолированные шаги.
4. **Расширяемость:** Добавить анализ тональности или выделение сущностей — это просто добавить новый класс в список шагов.

Предлагаю начать с **Phase 14.0** прямо сейчас. Это фундамент.
