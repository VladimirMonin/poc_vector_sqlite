# 🔍 POC: Семантический поиск на SQLite

> **Proof of Concept** — доказательство концепции векторного и гибридного поиска на базе SQLite + Vec + FTS5

## 🎯 Что это?

Легковесная альтернатива Postgres + PGVector для небольших проектов. Демонстрирует:

- ✅ **Векторный поиск** по смыслу текста (не только по ключевым словам)
- ✅ **Полнотекстовый поиск** (FTS5) для точного совпадения
- ✅ **Гибридный поиск** с алгоритмом RRF (Reciprocal Rank Fusion)
- ✅ **Фасетный поиск** по категориям

**Пример:** запрос "как написать цикл?" найдёт заметку "Циклы в Python", даже если слово "цикл" написано по-другому.

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
poetry install
```

### 2. Настройка окружения

```bash
# Создай .env файл
cp .env.example .env

# Добавь свой Gemini API ключ (получить: https://aistudio.google.com/apikey)
GEMINI_API_KEY=your_api_key_here
```

### 3. Запуск тестов

```bash
poetry run python main.py
```

Должно вывести результаты 4 типов поиска:
- Векторный поиск
- Полнотекстовый поиск
- Фасетный поиск (по категории)
- Гибридный поиск (RRF)

---

## 🛠️ Технологический стек

| Компонент | Назначение | Документация |
|-----------|-----------|--------------|
| **SQLite** | База данных | Встроенная, без сервера |
| **sqlite-vec** | Векторное расширение (C) | [asg017/sqlite-vec](https://github.com/asg017/sqlite-vec) |
| **Peewee** | Легковесная ORM | [coleifer/peewee](https://github.com/coleifer/peewee) |
| **Gemini API** | Получение эмбеддингов | [googleapis/python-genai](https://github.com/googleapis/python-genai) |
| **Poetry 2.0** | Управление зависимостями | [python-poetry.org](https://python-poetry.org/) |

---

## 📚 Документация

**→ [Полная документация по архитектуре](doc/architecture/00_overview.md)**

### Что внутри:

#### 🎓 Основы
1. **Основы эмбеддингов** — что это и как работает
2. **Gemini API** — модели, лимиты, task types
3. **SQLite-Vec** — хранение векторов в BLOB

#### 🔍 Поиск
4. **Типы поиска** — когда использовать векторный, а когда FTS
5. **Гибридный поиск** — алгоритм RRF

#### 🏛️ Архитектура
6. **Архитектура проекта** — разделение на слои, SOLID
7. **Поток данных** — от текста до результатов поиска

#### ✂️ Работа с большими документами
8. **Стратегия нарезки (Chunking)** — как обойти лимит 2000 токенов
9. **Parent-Child Retrieval** — ищем по чанкам, возвращаем целые документы

---

## 🏗️ Структура проекта

```
poc_vector_sqlite/
│
├── semantic_core/              # 🎁 Реюзабельный пакет
│   ├── database.py             # Инициализация SQLite + sqlite-vec
│   ├── embeddings.py           # Обёртка над Gemini API
│   ├── search.py               # Функции поиска (Parent-Child)
│   ├── services.py             # Сохранение с нарезкой на чанки
│   └── text_processing/        # Модуль нарезки текста
│       ├── base.py             # Абстрактный TextSplitter
│       └── simple_splitter.py  # SimpleTextSplitter
│
├── domain/                 # 📝 Бизнес-логика (заметки)
│   └── models.py           # Note (parent), NoteChunk (child)
│
├── tests/                  # 🧪 Тесты (pytest)
│   ├── conftest.py         # Фикстуры для тестов
│   └── test_splitter.py    # Тесты SimpleTextSplitter (17 тестов)
│
├── config.py               # ⚙️ Настройки (Pydantic)
├── main.py                 # 🚀 Демонстрация с реальными документами
└── doc/architecture/       # 📖 Документация (9 файлов)
```

---

## 💡 Ключевые особенности

### Реюзабельность

`semantic_core/` — это **готовый пакет**, который можно переиспользовать в других проектах:

```python
from semantic_core import HybridSearchMixin, init_database

class Article(HybridSearchMixin, BaseModel):
    title = CharField()
    content = TextField()
    
    def get_search_text(self):
        return f"{self.title} {self.content}"

# Автоматически получаешь:
results = Article.hybrid_search("запрос")
```

### Parent-Child архитектура

Работа с **большими документами** (>2000 токенов):

```python
# Проблема: документ на 9000 символов не влезает в лимит Gemini
document = "Очень длинная статья на 9000 символов..."

# Решение: нарезаем на чанки
splitter = SimpleTextSplitter(chunk_size=1000, overlap=200)
chunks = splitter.split_text(document)
# → 9 чанков по ~1000 символов с перекрытием

# Сохраняем с автоматической нарезкой и векторизацией
note = save_note_with_chunks(
    note_model=Note,
    chunk_model=NoteChunk,
    note_data={"title": "Статья", "content": document},
    splitter=splitter,
    generator=EmbeddingGenerator()
)
# → Note (parent) + 9 × NoteChunk (children) + 9 векторов

# Ищем по чанкам, получаем уникальные документы
results = vector_search_chunks(
    parent_model=Note,
    chunk_model=NoteChunk,
    query="ключевое слово",
    limit=5
)
# → [(Note#1, distance=0.29), (Note#2, distance=0.35), ...]
```

**Преимущества:**
- ✅ Обход лимита 2000 токенов
- ✅ Более точный поиск (находит конкретные фрагменты)
- ✅ GROUP BY note_id для уникальных результатов
- ✅ CASCADE DELETE для чистоты данных

### Производительность

**Параметры модели:**
- **2000 токенов** — лимит Gemini (решается через chunking)
- **768 измерений** — размер вектора (models/text-embedding-004)
- **Косинусное расстояние** — метрика схожести
- **RRF (k=60)** — комбинирование результатов

**Реальные данные POC:**
- 8 документов (2009-9022 символа)
- 54 чанка (среднее 6.8/документ)
- Векторный поиск: 0.05s
- Гибридный RRF: 0.08s

---

## 🎓 Для кого это?

- ✅ Разработчики, изучающие семантический поиск
- ✅ Стартапы, которым не нужен Postgres
- ✅ Проекты с небольшим объёмом данных (<100k записей)
- ✅ POC для проверки концепции перед масштабированием

---

## 📊 Примеры использования

### Векторный поиск по чанкам

```python
from semantic_core import vector_search_chunks, EmbeddingGenerator
from domain.models import Note, NoteChunk

# Находит по смыслу, даже в больших документах
results = vector_search_chunks(
    parent_model=Note,
    chunk_model=NoteChunk,
    query="как работает векторный поиск?",
    limit=5
)

for note, distance in results:
    print(f"{note.title}: {distance:.4f}")
# → Search Types: 0.2936
# → Overview: 0.3567
```

### Полнотекстовый поиск

```python
from semantic_core import fulltext_search_parents

# Точное совпадение слов через FTS5
results = fulltext_search_parents(
    parent_model=Note,
    query="Gemini API",
    limit=5
)

for note, rank in results:
    print(f"{note.title}: BM25={rank:.4f}")
```

### Гибридный поиск (RRF)

```python
from semantic_core import hybrid_search_rrf

# Комбинирует векторный + полнотекстовый поиск
results = hybrid_search_rrf(
    parent_model=Note,
    chunk_model=NoteChunk,
    query="эмбеддинги модель",
    limit=5
)

for note, rrf_score in results:
    print(f"{note.title}: RRF={rrf_score:.4f}")
# → Project Architecture: 0.0164
# → Embeddings Basics: 0.0161
```

### Сохранение с нарезкой

```python
from semantic_core import (
    save_note_with_chunks,
    SimpleTextSplitter,
    EmbeddingGenerator
)

# Читаем большой файл
with open("doc/architecture/07_data_flow.md") as f:
    long_text = f.read()  # 9022 символа

# Сохраняем с автоматической нарезкой
note = save_note_with_chunks(
    note_model=Note,
    chunk_model=NoteChunk,
    note_data={
        "title": "Data Flow",
        "content": long_text
    },
    splitter=SimpleTextSplitter(chunk_size=1000, overlap=200),
    generator=EmbeddingGenerator()
)

print(f"Создано {len(note.chunks)} чанков")
# → Создано 11 чанков
```

---

## 🤝 Вклад в проект

Это POC, но если хочешь улучшить:

1. Форкни репозиторий
2. Создай фичу-бранч (`git checkout -b feat/amazing-feature`)
3. Закоммить изменения (`git commit -m 'feat: Добавлена amazing-feature'`)
4. Запуш в бранч (`git push origin feat/amazing-feature`)
5. Открой Pull Request

---

## 📝 Лицензия

MIT License — используй как хочешь, но на свой страх и риск 😉

---

## 🔗 Полезные ссылки

- [Gemini API Documentation](https://ai.google.dev/gemini-api/docs)
- [sqlite-vec GitHub](https://github.com/asg017/sqlite-vec)
- [Peewee ORM Docs](http://docs.peewee-orm.com/)
- [Reciprocal Rank Fusion Paper](https://plg.uwaterloo.ca/~gvcormac/cormacksigir09-rrf.pdf)

---

**Начни с документации:** [doc/architecture/00_overview.md](doc/architecture/00_overview.md) 🚀
