# 🔍 POC: Семантический поиск на SQLite

> **Proof of Concept** — доказательство концепции векторного и гибридного поиска на базе SQLite + Vec + FTS5

## 🎯 Что это?

Легковесная альтернатива Postgres + PGVector для небольших проектов. Демонстрирует:

- ✅ **Векторный поиск** по смыслу текста (не только по ключевым словам)
- ✅ **Полнотекстовый поиск** (FTS5) для точного совпадения
- ✅ **Гибридный поиск** с алгоритмом RRF (Reciprocal Rank Fusion)
- ✅ **Фасетный поиск** по категориям

**Пример:** запрос "как написать цикл?" найдёт заметку "Циклы в Python", даже если слово "цикл" написано по-другому.

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
poetry install
```

### 2. Настройка окружения

```bash
# Создай .env файл
cp .env.example .env

# Добавь свой Gemini API ключ (получить: https://aistudio.google.com/apikey)
GEMINI_API_KEY=your_api_key_here
```

### 3. Запуск тестов

```bash
poetry run python main.py
```

Должно вывести результаты 4 типов поиска:
- Векторный поиск
- Полнотекстовый поиск
- Фасетный поиск (по категории)
- Гибридный поиск (RRF)

---

## 🛠️ Технологический стек

| Компонент | Назначение | Документация |
|-----------|-----------|--------------|
| **SQLite** | База данных | Встроенная, без сервера |
| **sqlite-vec** | Векторное расширение (C) | [asg017/sqlite-vec](https://github.com/asg017/sqlite-vec) |
| **Peewee** | Легковесная ORM | [coleifer/peewee](https://github.com/coleifer/peewee) |
| **Gemini API** | Получение эмбеддингов | [googleapis/python-genai](https://github.com/googleapis/python-genai) |
| **Poetry 2.0** | Управление зависимостями | [python-poetry.org](https://python-poetry.org/) |

---

## 📚 Документация

**→ [Полная документация по архитектуре](doc/architecture/00_overview.md)**

### Что внутри

1. **Основы эмбеддингов** — что это и как работает
2. **Gemini API** — модели, лимиты, task types
3. **SQLite-Vec** — хранение векторов в BLOB
4. **Типы поиска** — когда использовать векторный, а когда FTS
5. **Гибридный поиск** — алгоритм RRF
6. **Архитектура проекта** — разделение на слои, SOLID
7. **Поток данных** — от текста до результатов поиска

---

## 🏗️ Структура проекта

```
poc_vector_sqlite/
│
├── semantic_core/          # 🎁 Реюзабельный пакет
│   ├── database.py         # Инициализация SQLite + sqlite-vec
│   ├── embeddings.py       # Обёртка над Gemini API
│   └── search_mixin.py     # Mixin с методами поиска
│
├── domain/                 # 📝 Бизнес-логика (заметки)
│   └── models.py           # Note, Category, Tag
│
├── config.py               # ⚙️ Настройки (Pydantic)
├── main.py                 # 🧪 Песочница для тестов
└── doc/architecture/       # 📖 Документация
```

---

## 💡 Ключевые особенности

### Реюзабельность

`semantic_core/` — это **готовый пакет**, который можно переиспользовать в других проектах:

```python
from semantic_core import HybridSearchMixin, init_database

class Article(HybridSearchMixin, BaseModel):
    title = CharField()
    content = TextField()
    
    def get_search_text(self):
        return f"{self.title} {self.content}"

# Автоматически получаешь:
results = Article.hybrid_search("запрос")
```

### Производительность

- **2000 токенов** — лимит для векторизации одного документа (Gemini)
- **768 измерений** — размер вектора (models/text-embedding-004)
- **Косинусное расстояние** — метрика схожести
- **RRF (k=60)** — комбинирование результатов

---

## 🎓 Для кого это?

- ✅ Разработчики, изучающие семантический поиск
- ✅ Стартапы, которым не нужен Postgres
- ✅ Проекты с небольшим объёмом данных (<100k записей)
- ✅ POC для проверки концепции перед масштабированием

---

## 📊 Примеры использования

### Векторный поиск

```python
# Находит по смыслу, а не по словам
Note.vector_search("как написать цикл?", limit=5)
# → Найдёт "Циклы в Python", даже если слово "цикл" не упоминается
```

### Гибридный поиск (RRF)

```python
# Комбинирует векторный + полнотекстовый поиск
Note.hybrid_search("срочный скрипт", limit=5)
# → Ранжирует результаты по RRF алгоритму
```

### Фасетный поиск

```python
# Векторный поиск + фильтр по категории
python_category = Category.get(Category.name == "Python")
Note.vector_search("функции", limit=5, category_id=python_category.id)
```

---

## 🤝 Вклад в проект

Это POC, но если хочешь улучшить:

1. Форкни репозиторий
2. Создай фичу-бранч (`git checkout -b feat/amazing-feature`)
3. Закоммить изменения (`git commit -m 'feat: Добавлена amazing-feature'`)
4. Запуш в бранч (`git push origin feat/amazing-feature`)
5. Открой Pull Request

---

## 📝 Лицензия

MIT License — используй как хочешь, но на свой страх и риск 😉

---

## 🔗 Полезные ссылки

- [Gemini API Documentation](https://ai.google.dev/gemini-api/docs)
- [sqlite-vec GitHub](https://github.com/asg017/sqlite-vec)
- [Peewee ORM Docs](http://docs.peewee-orm.com/)
- [Reciprocal Rank Fusion Paper](https://plg.uwaterloo.ca/~gvcormac/cormacksigir09-rrf.pdf)

---

**Начни с документации:** [doc/architecture/00_overview.md](doc/architecture/00_overview.md) 🚀
