# 📁 Test Assets

Медиа-файлы и текстовые ассеты для тестирования SemanticCore.

## 🖼️ Изображения

| Файл | Описание | Использование |
|------|----------|---------------|
| `8k_japanese_walpaper.jpg` | Японский пейзаж (высокое разрешение) | Тест больших изображений, Vision API |
| `cat_photo.png` | Фото кота (PNG) | Базовый тест Vision API |
| `code_screen.jpg` | Скриншот кода | OCR, распознавание кода |
| `eiffel_tower.jpg` | Эйфелева башня | Тест достопримечательностей |
| `paris_street.jpg` | Парижская улица | Описание сцены |
| `red_car.jpg` | Красная машина | Распознавание объектов |
| `seq_django_diagram.png` | Sequence диаграмма Django | OCR диаграмм, архитектура |
| `small_icon.webp` | Маленькая иконка (WebP) | Тест малых изображений |
| `text_sign.jpg` | Вывеска с текстом | OCR текста на фото |

## 🎵 Аудио

| Файл | Описание | Использование |
|------|----------|---------------|
| `slides_ideas_audio.ogg` | Голосовые заметки про слайды (RU) | Транскрипция русской речи |

## 🎬 Видео

| Файл | Описание | Использование |
|------|----------|---------------|
| `module_init_demo.mp4` | Демо инициализации модуля (RU) | OCR + транскрипция |
| `new_year_greeting.mp4` | Новогоднее поздравление (RU) | Мультимодальный анализ |

## 📊 Использование в тестах

### E2E Audit Tests (`tests/e2e/audit/`)

```python
# Путь к ассетам
assets_path = Path(__file__).parent.parent / "asests"

# Изображения
image_files = list(assets_path.glob("*.jpg")) + list(assets_path.glob("*.png"))

# Аудио
audio_files = list(assets_path.glob("*.ogg")) + list(assets_path.glob("*.mp3"))

# Видео
video_files = list(assets_path.glob("*.mp4"))
```

### Типы тестов

| Тип | Файлы | Что тестируется |
|-----|-------|-----------------|
| **Vision** | `*.jpg`, `*.png`, `*.webp` | GeminiImageAnalyzer |
| **Audio** | `*.ogg`, `*.mp3`, `*.wav` | GeminiAudioAnalyzer |
| **Video** | `*.mp4`, `*.webm` | GeminiVideoAnalyzer |

## ⚠️ Важно

1. **Размер файлов**: Видео должны быть короткими (15-60 сек) для экономии токенов
2. **Язык**: Есть файлы на русском — тестируют мультиязычность
3. **Форматы**: Поддерживаются стандартные форматы (см. таблицы выше)
