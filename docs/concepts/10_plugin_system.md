---
title: "Plugin System & Extensibility"
description: "SOLID-архитектура с интерфейсами для расширения системы"
tags: ["solid", "interfaces", "plugins", "extensibility", "architecture"]
difficulty: "advanced"
related: ["01_embeddings", "02_vector_search", "08_rag_architecture"]
---

# Plugin System & Extensibility 🔌

> **Interface-based architecture**: заменяй компоненты без изменения кода.

---

## Проблема монолита 🎯

```
┌──────────────────────────────────────────┐
│            Монолитный код                │
│                                          │
│  Note ──▶ EmbeddingGenerator (Gemini)   │
│       ──▶ database.py (SQLite)           │
│       ──▶ HybridSearchMixin (Peewee)     │
│                                          │
│  Изменить одно = сломать всё!           │
└──────────────────────────────────────────┘
```

**Последствия**:
- Хочешь OpenAI вместо Gemini → переписывать половину кода
- Перейти с SQLite на PostgreSQL → переделывать интеграцию
- Тестировать без API → невозможно

---

## Решение: 4 слоя 📐

```
┌───────────────────────────────────────────────────────┐
│                    Pipeline Layer                      │
│              SemanticCore (оркестратор)               │
│                         │                              │
│                         ▼                              │
├───────────────────────────────────────────────────────┤
│                  Interfaces Layer                      │
│    BaseEmbedder │ BaseVectorStore │ BaseLLMProvider   │
│                         │                              │
│                         ▼                              │
├───────────────────────────────────────────────────────┤
│                Infrastructure Layer                    │
│  GeminiEmbedder │ PeeweeVectorStore │ GeminiLLM       │
│                         │                              │
│                         ▼                              │
├───────────────────────────────────────────────────────┤
│                    Domain Layer                        │
│         Document │ Chunk │ SearchResult               │
└───────────────────────────────────────────────────────┘
```

**Ключ**: слои общаются через **интерфейсы**, не напрямую.

---

## 8 интерфейсов системы 🔧

| Интерфейс | Роль | Ключевые методы |
|-----------|------|-----------------|
| `BaseEmbedder` | Векторизация текста | `embed_documents()`, `embed_query()` |
| `BaseVectorStore` | Хранение и поиск | `save()`, `search()`, `search_chunks()` |
| `BaseLLMProvider` | Генерация текста | `generate()`, `model_name` |
| `BaseSplitter` | Нарезка на чанки | `split()` |
| `BaseContextStrategy` | Формирование контекста | `form_vector_text()` |
| `DocumentParser` | Парсинг документов | `parse()` (Protocol) |
| `BaseChatHistoryStrategy` | Управление историей | `should_trim()`, `trim()` |
| `ChatMessage` | DTO сообщения | `role`, `content` |

---

## Пример: BaseEmbedder 🧠

```
┌─────────────────────────────────────────────┐
│              BaseEmbedder (ABC)             │
├─────────────────────────────────────────────┤
│ @abstractmethod                             │
│ embed_documents(texts: list[str])           │
│   -> list[list[float]]                      │
│                                             │
│ @abstractmethod                             │
│ embed_query(text: str)                      │
│   -> list[float]                            │
│                                             │
│ @property (optional)                        │
│ vector_dimensions: int                      │
└─────────────────────────────────────────────┘
           │
           │ implements
           ▼
┌─────────────────────────────────────────────┐
│           GeminiEmbedder                    │
│  • gemini-embedding-001                     │
│  • MRL: 768/1536/3072                       │
└─────────────────────────────────────────────┘
           │
           │ можно заменить на
           ▼
┌─────────────────────────────────────────────┐
│          OpenAIEmbedder (custom)            │
│  • text-embedding-3-small                   │
└─────────────────────────────────────────────┘
```

---

## Пример: BaseVectorStore 💾

```
┌─────────────────────────────────────────────┐
│            BaseVectorStore (ABC)            │
├─────────────────────────────────────────────┤
│ save(doc: Document, chunks: list[Chunk])   │
│ search(query_vec, limit, mode) -> Results  │
│ search_chunks(query_vec, limit) -> Chunks  │
│ bulk_update_vectors(chunk_ids, vectors)    │
│ get_all_documents() -> list[Document]      │
│ get_chunks_for_document(doc_id) -> Chunks  │
└─────────────────────────────────────────────┘
           │
           │ implements
           ▼
┌─────────────────────────────────────────────┐
│         PeeweeVectorStore                   │
│  • SQLite + sqlite-vec                      │
│  • Peewee ORM                               │
└─────────────────────────────────────────────┘
```

---

## Dependency Injection 💉

`SemanticCore` принимает интерфейсы в конструкторе:

```
┌─────────────────────────────────────────────┐
│             SemanticCore.__init__           │
├─────────────────────────────────────────────┤
│ embedder: BaseEmbedder                      │
│ store: BaseVectorStore                      │
│ splitter: BaseSplitter                      │
│ parser: DocumentParser                      │
│ context_strategy: BaseContextStrategy       │
└─────────────────────────────────────────────┘
```

**Преимущества DI**:
- Тестирование с моками без API
- Замена компонентов без изменения кода
- Конфигурация на уровне приложения

---

## Расширение системы 🔌

### Добавить новый Embedder

1. Создать класс, наследующий `BaseEmbedder`
2. Реализовать `embed_documents()` и `embed_query()`
3. Передать в `SemanticCore(embedder=MyEmbedder())`

### Добавить новый LLM Provider

1. Наследовать `BaseLLMProvider`
2. Реализовать `generate()` и свойство `model_name`
3. Передать в `RAGEngine(llm=MyProvider())`

### Добавить новый Storage

1. Наследовать `BaseVectorStore`
2. Реализовать 6 методов хранения
3. Передать в `SemanticCore(store=MyStore())`

---

## Тестирование с моками 🧪

```
┌─────────────────────────────────────────────┐
│              Production                      │
│                                              │
│  SemanticCore(                              │
│    embedder=GeminiEmbedder(api_key),        │
│    store=PeeweeVectorStore(db),             │
│  )                                           │
├─────────────────────────────────────────────┤
│                Testing                       │
│                                              │
│  SemanticCore(                              │
│    embedder=MockEmbedder(),     # Без API  │
│    store=InMemoryStore(),       # Без БД   │
│  )                                           │
└─────────────────────────────────────────────┘
```

---

## Protocol vs ABC 📜

| Подход | Когда использовать |
|--------|-------------------|
| **ABC** (Abstract Base Class) | Нужно наследование, общая логика |
| **Protocol** | Duck typing, структурная совместимость |

В проекте:
- `BaseEmbedder`, `BaseVectorStore`, `BaseLLMProvider` — **ABC**
- `DocumentParser` — **Protocol** (duck typing для парсеров)

---

## Связанные концепты 🔗

| Концепт | Как связан |
|---------|------------|
| [Embeddings](01_embeddings.md) | `BaseEmbedder` интерфейс |
| [Vector Search](02_vector_search.md) | `BaseVectorStore` интерфейс |
| [RAG Architecture](08_rag_architecture.md) | `BaseLLMProvider` интерфейс |
| [Chunking](04_chunking.md) | `BaseSplitter` интерфейс |

---

## Итог 📌

| Аспект | Реализация |
|--------|------------|
| Архитектура | 4 слоя: Domain → Interfaces → Infrastructure → Pipeline |
| Интерфейсы | 8 ABC/Protocol контрактов |
| DI | Конструктор `SemanticCore` принимает интерфейсы |
| Расширение | Наследование + реализация методов |
| Тестирование | Моки вместо реальных реализаций |
