---
title: Pre-Deploy Checklist
description: Проверки перед развёртыванием Semantic Core в продакшн
tags: [deployment, production, checklist, devops]
---

# Pre-Deploy Checklist ✅

Проверки перед развёртыванием. Каждый пункт — вопрос "а ты это сделал?".

## API & Secrets 🔐

| Проверка | Зачем | Команда |
|:---------|:------|:--------|
| API ключ в secrets manager | Не в .env на сервере | `echo $GEMINI_API_KEY \| head -c 10` |
| Batch ключ (если нужен) | Отдельный биллинг | `semantic config show` |
| Ротация ключей настроена | Безопасность | Проверить план ротации |
| Ключи не в логах | Утечка данных | Grep логи на `AIza` |

## Конфигурация ⚙️

| Проверка | Продакшн значение | Команда |
|:---------|:------------------|:--------|
| `log_level` | `INFO` (не DEBUG) | `semantic config show` |
| `embedding_dimension` | Консистентно с БД | Проверить существующие векторы |
| `media_rpm_limit` | ≤80% от лимита тарифа | Конфиг |
| `search_type` | `hybrid` для лучшего качества | Конфиг |

## База данных 💾

| Проверка | Зачем | Команда |
|:---------|:------|:--------|
| WAL mode включён | Concurrent reads | `sqlite3 db 'PRAGMA journal_mode;'` |
| Размер БД приемлем | Дисковое пространство | `ls -lh semantic.db` |
| Бэкап настроен | Disaster recovery | Проверить cron/script |
| VACUUM запланирован | Оптимизация размера | cron weekly |

```sql
-- Рекомендуемые PRAGMA для продакшна
PRAGMA journal_mode = WAL;
PRAGMA synchronous = NORMAL;
PRAGMA cache_size = -64000;  -- 64MB кэш
PRAGMA temp_store = MEMORY;
```

## Resilience 🛡️

| Проверка | Значение | Описание |
|:---------|:---------|:---------|
| Retry включён | max_retries=3-5 | Защита от временных ошибок |
| Backoff настроен | exponential | Не DDoS-им API |
| Rate limiter | ≤80% RPM | Запас до 429 |
| Timeout | 30-60 сек | Не висим вечно |

## Мониторинг 📊

| Метрика | Alert threshold | Действие |
|:--------|:----------------|:---------|
| API latency p99 | > 5s | Проверить rate limits |
| Error rate | > 5% | Проверить логи |
| Queue backlog | > 1000 | Добавить workers |
| Token usage/day | > budget | Уменьшить batch size |
| Disk usage | > 80% | VACUUM или увеличить |

## Перед деплоем 🚀

```bash
# 1. Проверить конфигурацию
semantic config show

# 2. Проверить подключение к API
semantic doctor

# 3. Проверить БД
sqlite3 semantic.db 'PRAGMA integrity_check;'

# 4. Тесты
pytest tests/ -v --ignore=tests/e2e

# 5. Dry-run индексации
semantic ingest ./sample.md --dry-run
```

## После деплоя 🔍

| Проверка | Как |
|:---------|:----|
| Приложение стартует | Логи без ошибок |
| Поиск работает | `semantic search "test"` |
| API отвечает | Healthcheck endpoint |
| Метрики идут | Dashboard/Grafana |

## Red Flags 🚩

**Немедленно откатывай если:**

- `429 Too Many Requests` в логах → уменьши RPM
- `SQLITE_BUSY` ошибки → включи WAL mode
- API ключ в логах → исправь redaction, ротируй ключ
- OOM (Out of Memory) → уменьши batch size
- Размер БД растёт неконтролируемо → проверь дубликаты

## См. также 🔗

- [Production Configuration](production.md) — детальная настройка
- [Конфигурация](../core/configuration.md) — все параметры
- [Error Codes](../../reference/error-codes.md) — troubleshooting
