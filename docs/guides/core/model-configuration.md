---
title: "Model Configuration"
description: "Выбор и настройка моделей Gemini для разных задач"
tags: ["models", "gemini", "embeddings", "configuration", "mrl"]
difficulty: "intermediate"
prerequisites: ["configuration"]
---

# Model Configuration 🎛️

> Выбор моделей Gemini для эмбеддингов, генерации и медиа.

---

## Модели эмбеддингов 🧠

| Модель | MRL размерности | Batch API |
|--------|-----------------|-----------|
| `gemini-embedding-001` | 768 / 1536 / 3072 | ✅ |
| `text-embedding-004` | 768 | ❌ Legacy |

**Рекомендация**: `gemini-embedding-001` — поддерживает MRL и Batch API.

---

## MRL (Matryoshka) 🪆

Одна модель, три размерности:

| Dimension | Качество | Размер индекса | Скорость |
|-----------|----------|----------------|----------|
| 768 | ~98% | Минимум | Быстрее |
| 1536 | ~99% | Средний | Средняя |
| 3072 | 100% | Максимум | Медленнее |

```toml
# semantic.toml
[embedding]
model = "models/gemini-embedding-001"
dimension = 768    # Экономия места
```

---

## Модели LLM 💬

| Модель | Назначение | Особенности |
|--------|------------|-------------|
| `gemini-2.5-flash` | RAG, чат | Быстрая, thinking mode |
| `gemini-2.5-pro` | Сложные задачи | Deep Think, дороже |
| `gemini-2.0-flash` | Совместимость | Предыдущее поколение |

```bash
# В чате
semantic chat --model gemini-2.5-pro
```

---

## Модели для медиа 🖼️

| Задача | Модель | Примечание |
|--------|--------|------------|
| Image | `gemini-2.5-flash` | Vision API |
| Audio | `gemini-2.5-flash` | Транскрипция |
| Video | `gemini-2.5-flash` | Кадры + аудио |

---

## Пресеты конфигурации 📦

### Максимальная экономия

```toml
[embedding]
dimension = 768

[gemini]
batch_key = "AIza..."    # Batch API = -50%
```

### Максимальное качество

```toml
[embedding]
dimension = 3072

# Для RAG
# semantic chat --model gemini-2.5-pro
```

---

## Миграция между моделями ⚠️

**Важно**: Нельзя смешивать эмбеддинги разных моделей!

При смене модели или размерности:

```bash
# 1. Бэкап
cp semantic.db semantic.db.backup

# 2. Очистка
semantic docs delete --all

# 3. Переиндексация
semantic ingest ./docs/ -r
```

---

## Следующие шаги 🔗

| Ресурс | Что узнаете |
|--------|-------------|
| [Embeddings Concept](../../concepts/01_embeddings.md) | Как работают эмбеддинги |
| [Batch Processing](../../concepts/06_batch_processing.md) | Экономия через Batch API |
