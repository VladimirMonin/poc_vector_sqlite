---
title: "CLI Usage"
description: "Полное руководство по командам Semantic Core CLI"
tags: ["cli", "commands", "terminal", "usage"]
difficulty: "beginner"
prerequisites: ["quickstart", "configuration"]
---

# CLI Usage 🖥️

> Все команды CLI с примерами и опциями.

---

## Что получим 🎯

- Знание всех доступных команд
- Примеры типичных сценариев
- Понимание опций и флагов

---

## Философия CLI 🎨

| Принцип | Реализация |
|---------|------------|
| **Rich Output** | Цветной вывод, таблицы, прогресс-бары |
| **Lazy Init** | БД создаётся только при первой записи |
| **JSON Mode** | `--json` для скриптов и пайплайнов |
| **Consistent** | Одинаковые флаги во всех командах |

---

## Глобальные опции 🌐

Доступны для всех команд:

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--db-path` | `-d` | Путь к SQLite базе |
| `--log-level` | `-l` | Уровень логов (TRACE/DEBUG/INFO/WARNING/ERROR) |
| `--json` | `-j` | JSON вывод для скриптов |
| `--verbose` | `-v` | Подробный вывод (= --log-level INFO) |
| `--version` | | Показать версию |
| `--help` | | Справка |

```bash
semantic --db-path ./custom.db search "query"
semantic -l DEBUG ingest ./docs/
semantic search "query" --json | jq '.results[0]'
```

---

## Команды управления 🛠️

### `semantic init`

Инициализация проекта:

```bash
semantic init                    # Создаёт БД, проверяет API
semantic init --db-path custom.db
```

### `semantic config show`

Показать текущую конфигурацию:

```bash
semantic config show             # Все настройки
semantic config show --json      # JSON формат
```

### `semantic doctor`

Диагностика системы:

```bash
semantic doctor                  # Проверка всех компонентов
```

Проверяет:

- ✅ sqlite-vec extension
- ✅ Gemini API доступность
- ✅ База данных
- ✅ Конфигурация

---

## Команда ingest 📥

Индексация документов:

> ⚠️ **Windows/Typer:** Опции ПЕРЕД путём! Это ограничение Typer/Click.

```bash
# Один файл
semantic ingest README.md

# Папка (рекурсивно) — опции ПЕРЕД путём!
semantic ingest --recursive ./docs/

# С паттерном файлов
semantic ingest --recursive -p "*.md" ./docs/

# Только текст (без медиа)
semantic ingest --no-media ./docs/
```

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--recursive` | `-r` | Обрабатывать вложенные папки |
| `--pattern` | `-p` | Glob-паттерн для файлов |
| `--no-media` | | Пропустить изображения/аудио/видео |
| `--batch` | `-b` | Использовать Batch API |

---

## Команда search 🔍

Семантический поиск:

```bash
# Базовый поиск
semantic search "как работает RRF"

# Больше результатов — опции ПЕРЕД запросом!
semantic search --limit 20 "query"

# Тип поиска
semantic search --type vector "query"   # Только векторный
semantic search --type fts "query"      # Только FTS5
semantic search --type hybrid "query"   # Гибридный (default)

# Порог релевантности
semantic search --threshold 0.5 "query"
```

| Опция | Короткая | Описание |
|-------|----------|----------|
| `--limit` | `-l` | Количество результатов (default: 10) |
| `--type` | `-t` | Тип поиска: vector/fts/hybrid |
| `--threshold` | `-T` | Минимальный score (0.0-1.0) |
| `--k` | `-k` | Параметр RRF k (default: 60) |

---

## Команда docs 📚

Управление документами:

```bash
# Список всех документов
semantic docs list

# Статистика
semantic docs stats

# Удаление
semantic docs delete --id 42
semantic docs delete --all        # Всё!
```

---

## Команда chat 💬

Интерактивный RAG-чат:

```bash
# Запуск чата
semantic chat

# С настройками
semantic chat --model gemini-2.5-pro
semantic chat --context 10        # Больше контекста
semantic chat --search vector     # Тип поиска
```

Подробнее: [RAG Chat Guide](rag-chat.md)

---

## Команды Queue/Worker 📦

Для Batch API обработки:

```bash
# Добавить задачи в очередь
semantic queue add ./docs/ -p "*.md"

# Показать статус очереди
semantic queue status

# Запустить воркер
semantic worker start

# Запустить с лимитом
semantic worker start --max-tasks 100
```

| Команда | Описание |
|---------|----------|
| `queue add` | Добавить файлы в очередь |
| `queue status` | Статус очереди |
| `queue clear` | Очистить очередь |
| `worker start` | Запустить обработку |
| `worker stop` | Остановить воркер |

---

## JSON Output 📄

Для скриптов и автоматизации:

```bash
# Поиск → JSON
semantic search "query" --json

# Список документов → JSON
semantic docs list --json

# Пример с jq
semantic search "query" --json | jq '.results[] | .title'
```

Структура JSON:

```json
{
  "success": true,
  "results": [
    {
      "id": 1,
      "title": "Document",
      "score": 0.92,
      "snippet": "..."
    }
  ],
  "meta": {
    "query": "...",
    "search_type": "hybrid",
    "total": 5
  }
}
```

---

## Примеры сценариев 📝

### Индексация проекта

```bash
# Markdown + код
semantic ingest ./docs/ -p "*.md" -r
semantic ingest ./src/ -p "*.py" -r
```

### Поиск и фильтрация

```bash
# Топ-3 результата с высоким порогом
semantic search "authentication" -l 3 -T 0.7
```

### Пайплайн с jq

```bash
# Получить только titles
semantic search "query" --json | jq -r '.results[].title'

# Посчитать результаты
semantic search "query" --json | jq '.meta.total'
```

---

## Частые проблемы ⚠️

| Проблема | Решение |
|----------|---------|
| `Command not found: semantic` | Активируйте `poetry shell` |
| `No results found` | Сначала запустите `ingest` |
| `Rate limit` | Используйте `--batch` или подождите |
| `JSON parse error` | Проверьте `--json` флаг |

---

## Следующие шаги 🔗

| Гайд | Что узнаете |
|------|-------------|
| [RAG Chat](rag-chat.md) | Интерактивный чат |
| [Media Processing](media-processing.md) | Обработка изображений/аудио |
| [CLI Reference](../../reference/cli-commands.md) | Полный справочник команд |
