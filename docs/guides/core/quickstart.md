---
title: "Quickstart"
description: "Первые шаги с Semantic Core за 5 минут"
tags: ["quickstart", "installation", "getting-started"]
difficulty: "beginner"
prerequisites: []
---

# Quickstart 🚀

> От установки до первого поиска за 5 минут.

---

## Что получим 🎯

- Работающую базу знаний из ваших документов
- Семантический поиск по смыслу
- CLI команды для повседневной работы

---

## Требования 📋

| Требование | Версия | Примечание |
|------------|--------|------------|
| Python | 3.13+ | ⚠️ 3.14 не поддерживается на Windows |
| pip/uv | любая | uv рекомендуется |
| Gemini API key | [Получить](https://aistudio.google.com/app/apikey) | Бесплатный tier |

---

## Шаг 1: Установка 🛠️

### macOS / Linux

```bash
# Клонирование
git clone https://github.com/your/poc_vector_sqlite.git
cd poc_vector_sqlite

# Создание окружения
python -m venv .venv
source .venv/bin/activate

# Установка
pip install -e ".[media]"
```

### Windows (PowerShell)

```powershell
# Клонирование
git clone https://github.com/your/poc_vector_sqlite.git
cd poc_vector_sqlite

# Создание окружения
python -m venv .venv
.\.venv\Scripts\Activate.ps1

# Установка
pip install -e ".[media]"
```

---

## Шаг 2: API ключ 🔑

### macOS / Linux

```bash
export SEMANTIC_GEMINI_API_KEY="ваш_ключ"
```

### Windows (PowerShell)

```powershell
$env:SEMANTIC_GEMINI_API_KEY = "ваш_ключ"
```

### Альтернатива: semantic.toml

```toml
# semantic.toml (в корне проекта)
[gemini]
api_key = "ваш_ключ"
```

> ⚠️ **Важно:** Все переменные окружения требуют префикс `SEMANTIC_`

---

## Шаг 3: Инициализация 🗃️

```bash
# Создаёт БД и проверяет окружение
semantic init
```

Ожидаемый вывод:

```
✅ База данных создана: semantic.db
✅ Gemini API доступен
✅ Готово к работе!
```

---

## Шаг 4: Индексация 📥

> ⚠️ **Windows:** Опции (`--recursive`) ПЕРЕД путём!

```bash
# Один файл
semantic ingest README.md

# Папка с документами (рекурсивно)
semantic ingest --recursive ./docs/

# Только Markdown файлы
semantic ingest --recursive -p "*.md" ./docs/
```

Ожидаемый вывод:

```
📥 Обработка документов...
  ✅ README.md (5 чанков)
  ✅ docs/guide.md (12 чанков)
📊 Всего: 2 документа, 17 чанков
```

---

## Шаг 5: Поиск 🔍

```bash
# Семантический поиск
semantic search "как работает гибридный поиск"
```

Ожидаемый вывод:

```
🔍 Результаты поиска:

1. [0.92] docs/architecture.md
   Гибридный поиск объединяет векторный и полнотекстовый...

2. [0.85] README.md  
   Поддерживает три режима: vector, fts, hybrid...
```

---

## Проверка ✅

```bash
# Диагностика системы
semantic doctor
```

Все зелёные ✅ = система работает.

---

## Частые проблемы ⚠️

| Проблема | Решение |
|----------|---------|
| `API key not configured` | Используйте `SEMANTIC_GEMINI_API_KEY` (с префиксом!) |
| `Missing argument 'PATH'` | Опции перед путём: `semantic ingest --recursive docs` |
| `Pillow build failed` (Windows) | Используйте Python 3.13, не 3.14 |
| `No documents found` | Проверьте путь к файлам |
| `Rate limit exceeded` | Подождите минуту или batch mode |

---

## Следующие шаги 🔗

| Гайд | Что узнаете |
|------|-------------|
| [Configuration](configuration.md) | Настройка semantic.toml |
| [CLI Usage](cli-usage.md) | Все команды CLI |
| [RAG Chat](rag-chat.md) | Интерактивный чат с базой знаний |

---

## Python API (альтернатива CLI)

```python
from semantic_core import SemanticCore

# Инициализация
core = SemanticCore.from_config()

# Индексация
core.ingest_file("README.md")

# Поиск
results = core.search("гибридный поиск", limit=5)
for r in results:
    print(f"[{r.score:.2f}] {r.document.title}")
```

---

**Готово!** Теперь у вас есть работающий семантический поиск. 🎉
